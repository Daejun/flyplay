# 실행 계획 — 버섯체에 시각 연결(A) → 밀폐된 방 길찾기(B)

작성 2026-09-17. 근거는 [RESEARCH.md](RESEARCH.md) 13절(문헌 검토)과 이 문서 "이미 확인한 것"의
오늘 측정값입니다. **2026-09-17 승인("진행"), A-1부터 진행 중.** 단계가 끝날 때마다 해당
절에 ✅와 측정값을 적습니다.

## Context

사용자가 정한 순서는 "① 버섯체에 시각 연결 → ② 냄새 나는 설탕물과 방해물이 있는 밀폐된
방에서 길 찾기"입니다. 문헌 검토 결과 둘은 **이어지는 단계가 아니라 다른 질문**이었고,
추천 순서 A → B-1 → B-2 → B-3을 따르기로 했습니다.

- **A — 버섯체 시각.** "초파리가 색과 밝기의 의미를 버섯체로 배운다"를 Vogt et al. 2014·2016
  방식으로 재현합니다. 후각 버섯체의 구획·도파민·기억 저장을 그대로 공유하므로 규모가 작고,
  결과를 해부·행동 문헌과 직접 비교할 수 있습니다.
- **B — 밀폐된 방.** 방 실험을 물리·행동·신경 근거가 있는 부품으로 쌓습니다. 오늘 측정에서
  **벽에 부딪힌 초파리가 0.35–2.3초 안에 뒤집힌다**는 것이 나와서, 맨 앞에 "몸이 방을
  견디는가" 관문(B-0)을 넣었습니다.

성공은 시연이 아니라 **대조군과의 비교**로 판정합니다. A는 가소성 없음·전역 도파민·시각 KC
차단과, B는 정답 방향을 아는 오라클 조향과 비교합니다. 파라미터를 목표 수치에 맞추지
않습니다.

---

## 이미 확인한 것

### 오늘 측정 (스크래치 스크립트, 프로젝트 파일 변경 없음)

**시각** — 하부 시야 368낱눈 평균, 노랑형/옅은형 순

| 항목 | 결과 |
|---|---|
| 바닥 색판을 눈이 읽나 | **읽습니다.** 파랑 판 0.063/0.479, 초록 판 0.485/0.060, 회색 0.272/0.268, 흰색 0.485/0.479, 기본 체커 바닥 0.21/0.20 |
| 좌우 색 신호 | 왼쪽 파랑·오른쪽 초록 바닥 → 왼눈 0.071/0.465, 오른눈 0.475/0.077 |
| 상부 시야 | 바닥 색과 무관하게 0.99 (흰 하늘) |
| 색과 밝기 | 어두운 파랑 0.048/0.221 — 두 종류의 **비**가 색, **평균**이 밝기. 파랑·초록 판은 두 종류 평균이 **0.271 / 0.273**으로 모델 기준 거의 같은 밝기 |
| 실행 중 색 바꾸기 | `mj_model.geom_rgba`를 바꾸면 다음 렌더에 바로 반영 → 한 번 컴파일한 장면으로 훈련(바닥 전체 한 색)과 시험(두 색)을 오감 |
| 렌더 비용 | 25.3 ms/회 (정상 상태) |

**몸**

| 항목 | 결과 |
|---|---|
| 속도 | 대칭 하행 신호 0 → 0.5 mm/s (선 채로 거의 정지), 0.4 → 7.3, 1.0 → 18.6, 1.6 → 27.5 mm/s. 거의 선형 |
| 최대 선회 | [0.4, 1.6] → 228°/s, 반경 4.7 mm. [0.7, 1.3] → 113°/s, 반경 9.3 mm |
| **벽 충돌** | 높이 4·8 mm 벽에 정면·45°로 걸어 들어가면 **0.35–2.3초 안에 벽을 타다 뒤집힘** |
| 미끄러운 벽 | 벽 마찰을 0.1·0.01로 낮춰도 정면·45°는 못 막음. 스치듯 75°에서만 미끄러져 지나감 → **닿기 전에 돌아서는 반사가 필수** |
| 부착력 끄기 | `Walker.reset`이 부착 상태를 항상 설정해서 `adhesion=False` 파리는 KeyError |

### 코드·라이브러리 확인

| 항목 | 결과 |
|---|---|
| 색맹 지점 | `RetinaFeatures.intensity`와 뷰어 눈 모자이크가 두 채널을 max로 합침 |
| 망막 | 한 눈 721낱눈(옅은형 216). 노랑형은 초록, 옅은형은 파랑 채널만 읽고 빨강은 안 읽음. 눈 렌더러는 geom 그룹 1·2를 끔 |
| 하늘·조명 | 흰색 단색 하늘, 헤드라이트 하나(주변 0.5, 확산 0.6) |
| 충돌 | 벽을 `ground_geoms`에 넣고 `add_fly` 하면 충돌함. 단 `leg_contacts`(`ground_only=True`)가 벽 접촉도 지면으로 셈 |
| FlyGym 1.x 플룸 | phiflow로 바람 + 이류만(확산 호출 없음, 장애물 없음), HDF5 격자 조회. **phiflow·h5py는 어느 venv에도 없음** |
| 1.x 플룸 추적 제어기 | Demir 2020 기반 전진/좌·우회전/정지 상태기계, 전이율 파라미터 있음. 회전 명령 (−0.4, 1.2)은 이 Walker의 0.4 하한과 맞지 않아 옮겨 써야 함 |
| 1.x 경로 적분 | 다리 보폭 변위(접지 마스크) → 선형회귀로 방향·거리 변화 추정, 훈련 R² 0.96/0.97 |
| scipy 1.18 | 정상상태 편미분방정식을 희소 행렬 직접 풀이로 계산 가능. 새 의존성 불필요 |

### 문헌 (에이전트가 본문으로 확인한 것만)

| 출처 | 설계에 쓰는 사실 |
|---|---|
| [Vogt et al. 2014](https://elifesciences.org/articles/02395) | 색은 **바닥 아래 LED**(452/520 nm). 훈련: 아레나 전체를 초록·파랑 60 s씩 교대, 사이 12 s 어둠, 4회. 처벌: CS+ 60 s 안에 1 s × 12회. 보상: 말린 설탕. 시험: 훈련 60 s 뒤 **대각 사분면**에 두 색, 90 s. 상호 두 집단, `LI = [PI_G(G+B−) − PI_G(G−B+)]/2`, 음수 = 회피. 1회 훈련으로 이미 유의, 30–120 V에서 포화. 처벌은 PPL1, 보상은 PAM이 필요하고 **냄새와 같은 PPL1**. γ엽 KC 필요. **밝기 대조군 없음, 기억 점수는 그래프로만 있음** |
| [Vogt et al. 2016](https://elifesciences.org/articles/14009) | γd KC(약 75개)가 파랑·초록 빛에 반응, 차단하면 색 기억만 손상. **색 과제는 VPN-MB1**(메듈라 M8, 세포 다수, 각 약 20 카트리지)**, 밝기 과제(초록 10:1)는 VPN-MB2**(M7 넓은 수용장, 1–3개)가 필요 — 이중 해리 |
| [Ganguly et al. 2024](https://pmc.ncbi.nlm.nih.gov/articles/PMC11228034/) | KC의 8%가 시각. γd KC 하나당 VPN 입력 **1–7개, 중앙값 3**. 배선은 무작위와 구별 안 됨. 메듈라 VPN은 **하부 시야**를 덮음. 시각·후각 KC가 DAN과 MBON을 공유 |
| [Li et al. 2020](https://elifesciences.org/articles/62576) | hemibrain γd 99개, α/βp 60개. MBON 34종 중 22종이 부채꼴체 접선 뉴런으로 출력(주로 4·5층) |
| [Wolf et al. 1998](https://pmc.ncbi.nlm.nih.gov/articles/PMC311233/) | 버섯체 없이 시각 과제 9개 정상(주로 비행 시뮬레이터). Vogt 2014는 걷는 과제·조작적 요소 차이로 설명 |
| [Okray et al. 2023](https://pmc.ncbi.nlm.nih.gov/articles/PMC10208976/) | 냄새+색 복합 학습 강화 — **2026년 철회**(전압 영상 재현 실패, 행동은 재현됐다고 저자 주장). 이 계획에서는 쓰지 않음 |
| [Álvarez-Salvado et al. 2018](https://pmc.ncbi.nlm.nih.gov/articles/PMC6103744/) (저자 코드 포함) | 냄새 → 적응(τ 9.8 s)·압축 `C = c/(c+0.01+A)` → ON(τ 0.72 s), OFF(느린 4.84 s − 빠른 0.62 s 저역통과). 속도 `v = 6(1 + 0.45·ON − 0.8·OFF)` mm/s, 회전 확률 `0.12 − 0.03·ON + 0.75·OFF` (20 ms당), 바람 거슬러 `−5·ON·sin θ` °/샘플. 좌우 비교 항은 효과가 작음. **바람 감각 없이 좌우 비교만으로는 발생원을 못 찾음** |
| [Demir et al. 2020](https://pmc.ncbi.nlm.nih.gov/articles/PMC7609052/) | 난류 플룸: 만남 빈도로 치우친 30° 회전(시간당 약 1.3회), 걷기↔정지 전이율(λ₀ 0.78/−0.61/0.25 s; 0.29/0.41/0.52 s). NeuroMechFly v2가 값을 바꿔 재사용(도달 9/100) |
| [Tao et al. 2020](https://pmc.ncbi.nlm.nih.gov/articles/PMC7105121/) | **정지 공기**에서 냄새 구역에 끌리는 방식은 경계에서의 감속과 안쪽으로 꺾는 급회전. 좌우 비교는 주된 기작이 아님 |
| [Mussells Pires et al. 2024](https://pmc.ncbi.nlm.nih.gov/articles/PMC10881393/), [Westeinde et al. 2024](https://pmc.ncbi.nlm.nih.gov/articles/PMC10881397/) | PFL3: `r = f(cos(H − H_i) + d·cos(G − G_i))`, 조향 = 오른쪽 합 − 왼쪽 합 ∝ (머리 방향 − 목표)의 사인, 목표는 **FC2**에 저장. PFL2는 반대 방향 근처에서 이득을 올림 |
| [Matheson et al. 2022](https://pmc.ncbi.nlm.nih.gov/articles/PMC9360402/) | 냄새 → 부채꼴체 접선 뉴런 FB5AB(MBON 입력 받음) → hΔC → 바람 거슬러 가기. 부록: 드라이버가 hΔK를 주로 표지했을 수 있음 |
| [Siliciano et al. 2026](https://www.biorxiv.org/content/10.1101/2025.02.15.638426v1.full) (프리프린트 본문) | 냄새 통로 가장자리 추적: FC2가 가장자리로 돌아갈 방향을 기억, 교차 방향과의 선형 혼합으로 갱신, EPG 차단 시 사라짐 |
| [Kuntz et al. 2012](https://pmc.ncbi.nlm.nih.gov/articles/PMC3407938/) | 우회 과제: 목표 줄무늬가 사라지고 방해물이 나타났다가 사라지면 원래 목표로 복귀(야생형 80%, 우연 58%). 기억 지속은 "수 초"(Neuser 2008 초록) |
| [Soibam et al. 2012](https://pmc.ncbi.nlm.nih.gov/articles/PMC3345355/) | 8.4 cm 아레나에서 시간의 90–95%를 바깥 1/3에서 보냄, 경계를 **봐야** 생김. 걷는 초파리의 정량적 장애물 회피 규칙은 **찾지 못함**(오히려 물체에 다가감) |
| [Corfas et al. 2019](https://pmc.ncbi.nlm.nih.gov/articles/PMC6532790/), [Behbahani et al. 2021](https://pmc.ncbi.nlm.nih.gov/articles/PMC8551043/) | 설탕 뉴런 자극이 국소 탐색을 촉발(1.85 mm 통로 미로에서도 성공). 먹이에서 경로 적분기를 0으로 맞추고 왕복 거리를 점점 늘림 |
| [Tang et al. 2015](https://acp.copernicus.org/articles/15/5585/2015/acp-15-5585-2015-supplement.pdf) | 확산계수(298 K): 아세트산 0.124, 에탄올 0.129, 에틸아세테이트 약 0.086 cm²/s |
| 장애물 + 냄새 | 걷는 초파리(개미·바퀴 포함)가 실제 장애물을 돌아 냄새를 따라간 연구는 **검색으로 찾지 못함** |

---

## 전체 구조

```
A  겹눈 (721×2; 노랑형=초록, 옅은형=파랑)
    └ 하부 시야 → VPN: 색 대립(좁은 수용장, 다수) / 밝기(넓은 수용장, 소수)   visual_pathway.py
         └ γd형 시각 KC 100개 (눈마다 같은 배선, KC당 VPN 3개)
후각 KC 2000개 ─┐
시각 KC 100개 ──┴→ 같은 approach/avoid 구획, 같은 처벌·보상 도파민          mushroom_body.py
                    ├ 냄새별 가치 × 냄새 좌우차 → 화학주성 조향   (기존)
                    └ 왼눈 가치 − 오른눈 가치 → 시각 조향         (신규)  → 하행 신호

B  방: 바닥 + 벽 + 장애물 (충돌은 안전망일 뿐, 닿기 전에 반사가 막음)         room.py
    냄새장: 정상상태 이류–확산, 벽·장애물 무유속, 흡수, 바람 유무            odor_grid.py
    ├ 근접 반사 / 벽 따라가기          (최우선)
    ├ 전략 상태기계: 바람 거슬러 가기 / 정지·회전 / 국소 탐색              strategies.py
    ├ 중심복합체형: 머리 방향 고리 + 목표 기억 + PFL3 조향                 central_complex.py
    └ 좌우 냄새 비교 (약한 채널)                                          → 하행 신호
```

---

# A. 버섯체에 시각 연결 — Vogt 2014·2016 재현

## A-1. 시각 전단 (`flyplay/visual_pathway.py`)

`OlfactoryFrontEnd`와 같은 모양으로 만듭니다. 입력은 `get_ommatidia_readouts()`의
`(2, 721, 2)`, 출력은 눈마다 시각 KC 부호입니다. 전부 numpy.

| 층 | 구현 | 근거 |
|---|---|---|
| 광수용기 | 낱눈마다 자기 채널(노랑형→0, 옅은형→1). **하부 시야만** 사용 | 메듈라 VPN이 하부 시야를 덮음(Ganguly 2024), 색은 바닥 아래에서 옴(Vogt 2014) |
| 색 VPN | 하부 시야를 20낱눈 조각으로 덮는 유닛 24개. 조각 안 `(P−Y)/(P+Y)`를 파랑쪽·초록쪽으로 정류(회색에서 0) | VPN-MB1: 세포 다수, 각 약 20 카트리지 (Vogt 2016). 비율이라 밝기에 불변 — 모델 선택 |
| 밝기 VPN | 하부 시야 `(평균Y + 평균P)/2`의 로그를 로그 간격 4개 대역(0.02–0.54)으로 범위 부호화 | VPN-MB2: 넓은 수용장, 1–3개 (Vogt 2016). R1–6이 없어 두 종류 평균으로 대신 |
| 시각 KC | γd형 **100개**, KC마다 VPN **3개**(로그정규 가중), 그중 **절반의 세포에 밝기 입력 1개**, top-k 5개 | hemibrain γd 99, 입력 중앙값 3 (Ganguly 2024). 밝기 입력 비율은 아래 측정으로 정함 |

**✅ 구현·측정 완료** (`flyplay/visual_pathway.py`, 배선 시드 16개, 부호 간 코사인):

| 설계 | 파랑–초록 | 밝은–10배 어두운 초록 | 파랑–어두운 파랑 |
|---|---:|---:|---:|
| 비율 색 + ON/OFF 한 쌍 (버림) | 0.000 | 0.88 (0.59–1.00) | 0.98 |
| 대립 색 + 밝기 대역 4개 (버림) | 0.000 | 0.83 (0.57–1.00) | 0.92 |
| **채택: + 세포 절반에 밝기 입력** | **0.000** | **0.71 (시드별 0.15–0.98)** | **0.85** |

**측정 중 바뀐 것 — VPN은 낱눈 줄 분위 0.6–0.8의 띠만 봅니다.** 하부 시야 전체(분위 0.5 이상)를
쓰자 어두운 초록이 탈색돼 보였습니다(옅은형 비율 0.30, 원래 0.15). 검은 바닥으로 줄별로 재 보니
수평선 쪽 줄(0.5–0.6)은 하늘이 섞여 0.121/0.067, 맨 아래 줄(0.8–1.0)은 자기 다리가 보여 최대
0.076이었고, 가운데 띠(0.6–0.8, 낱눈 150개)만 정확히 0.000이었습니다. 이 띠에서는 파랑
0.027/0.486과 초록 0.486/0.027이 **정확히 거울상이라 모델 밝기가 똑같고**(0.257), 렌더한 바닥과
합성 바닥의 KC 부호가 코사인 0.99–0.999로 일치합니다. 위 표는 띠 기준으로 다시 잰 값입니다.

색 유닛이 24 대 2로 많아서 무작위 배선이면 부호가 밝기를 거의 못 봅니다. 그러면 Vogt 2016의
밝기 과제를 배울 수 없습니다. VPN-MB2는 1–3개뿐인데 밝기 과제에 γd가 필요하므로, 적은 수가
많은 γd에 닿는다고 추론해 세포 절반에 밝기 입력을 줬습니다. 대가는 색 기억의 밝기 일반화
감소와 큰 시드 편차(최악 시드는 밝기를 거의 못 봄)입니다. 두 차단 항등식은 모든 시드에서
정확히 성립하고, 한 번 계산에 0.065 ms입니다.

좌우 눈은 **같은 배선**(거울 대칭 버섯체)을 씁니다. 조향은 눈별 부호를 따로 읽고, 학습은 두
눈 부호의 평균으로 합니다. α/βp형(상부 시야, 로브라 VPN)은 A 범위 밖이고 B-3 지표 학습
때 다시 봅니다.

**통과 기준** (`scripts/13_mb_check.py` 7절, 측정값으로 만든 합성 망막 배열, 몇 초):
- 같은 밝기의 파랑 대 초록: 시각 KC 부호 코사인 **< 0.2**
- 색 VPN을 끄면 같은 밝기의 파랑·초록 부호가 **정확히 같음**(항등식)
- 밝기 VPN을 끄면 밝기만 다른 같은 색 부호가 **정확히 같음**(항등식)
- 밝기 10:1 차이의 같은 색: 코사인은 **측정해서 기록**(기준 없음 — 밝기 학습이 가능한 정도를 보여줄 뿐)
- 활성 KC 수가 정확히 k. 100개에서 k=5가 동률에 취약하면 측정 후 10%로 올림

## A-2. 버섯체 확장 (`flyplay/mushroom_body.py`)

- `MushroomBody(front_end, compartments, *, shared_dan, visual=None)` — KC 벡터를
  `[후각 2000 | 시각 100]`으로 이어 붙이고, 구획 가중치 길이도 2100이 됩니다. 모달리티마다
  따로 합 1로 정규화합니다(두 감각이 MBON에 같은 비중 — 모델 선택, 측정으로 확인).
- `step(concentration, dan, dt, feedback=None, readouts=None)`. 학습은 어느 감각이든 KC가
  활성일 때만.
- **`visual=None`이면 지금과 비트 단위로 같아야 합니다.** 기존 16항목이 그대로 통과하고, 시드 0의
  첫 시험 선호도 −0.516이 그대로여야 합니다.
- 기억 파일 형식 2: 모달리티별 KC 수·배선 시드 저장. 형식 1(기존 5개)을 시각 파리에 불러오면
  시각 가중치는 `w0`("시각 경험 없는 파리")로 채우고 그렇게 표시합니다. 시각 가중치가 있는 파일을
  시각 없는 파리에 불러오면 거부합니다.

**통과 기준** (`13_mb_check.py` 7절 이어서):
- 파랑 + 처벌 도파민 20시행 뒤 냄새 A의 가치 변화 **정확히 0**(서로 다른 KC)
- 공유 구획: 파랑 처벌 뒤 approach 구획의 시각 KC 가중치만 내려감
- 전역 도파민 조건에서 파랑/초록 가치 분리 **정확히 0**(기존 항등식의 시각판)

**✅ 완료.** `13_mb_check.py` **29/29**(기존 16 + 시각 13). 냄새 실험은 **바이트 단위로 동일** —
시드 0 첫 시험 선호도 −0.5161, 차등+소거 파리 5시행의 경로·가중치 해시까지 변경 전후 파일 비교.
계획과 달라진 점: 감각마다 합 1로 두면 시각 세포 5개가 후각 세포 100개와 같은 무게를 가져 세포당
20배 빨리 배우므로, 시각 부호를 `k_시각/k_후각 = 0.05`로 줄여 **활성 세포 하나의 크기를 같게**
했습니다(측정 비 1.00). 그래서 시각 기억이 움직일 수 있는 가치 폭은 ±0.05이고, 조향은 이것으로
나눈 정규화 가치(−1 = 그 색 완전 억압)를 씁니다. 파랑 20시행 뒤 −0.033(최대의 66%, 냄새 69%).

## A-3. 색 조건화 과제 (`flyplay/conditioning.py`, `flyplay/arena.py`, `flyplay/build.py`)

**장면.** 바닥 위 0.02 mm에 시각 전용 타일(그룹 0, 충돌 없음)을 깔고 `geom_rgba`로 색을
바꿉니다. 벽이 없어야 하므로(B-0 측정) Vogt의 사분면 아레나를 **체커보드 타일**로 옮깁니다 —
대각 타일이 같은 색이라 어느 교차점에서 보든 Vogt의 사분면 배치와 같습니다. 타일 크기는
20 mm에서 시작해 측정으로 정합니다.

**프로토콜 대응**

| Vogt 2014 | 모델 |
|---|---|
| 아레나 전체 초록 60 s ↔ 파랑 60 s, 사이 12 s 어둠, 4회 | 같음. 어둠 구간은 물리를 돌리지 않고 복원항만 해석적으로 적용 |
| CS+ 60 s 안에 1 s 충격 × 12 | CS+ 구간에 1 s 도파민 × 12, 진폭 1.0 (냄새 실험의 처벌 한 단위) |
| 보상: 말린 설탕 | 보상 도파민 → avoid 구획 |
| 훈련 60 s 뒤 대각 사분면 시험 90 s | 복원항 60 s 적용 뒤 체커보드 시험 90 s |
| 상호 두 집단, `LI = [PI_G(G+) − PI_G(B+)]/2` | 같은 식. 파리마다 G+ 또는 B+ |
| 흥분용 90 V 펄스 | 생략(모델에 각성 없음) |

**조향.** 시각 가치를 눈별로 읽어 `turn = gain_v · (V_왼눈 − V_오른눈)`. 냄새 조향과 더해서
씁니다. 냄새에서 겪은 "정면 문제"가 여기도 있습니다 — 경계를 정면으로 넘을 때는 좌우차가
없습니다. 정지 공기의 냄새 구역에서도 초파리를 끄는 것은 좌우 비교보다 **경계에서의 감속과
안쪽 급회전**이었습니다(Tao et al. 2020). 그래서 학습 전에 **천장 시험**부터 합니다.

1. **손으로 만든 기억으로 천장 측정.** 파랑의 approach 시각 가중치를 50% 눌러 놓은 파리로 시험만
   돌려, 이 몸과 이 조향이 낼 수 있는 LI를 잽니다. 좌우 비교만 / + 속도 조절(싫은 색 위에서
   빨라짐) / + 회전 증가를 비교하고, **LI ≤ −0.3을 내는 가장 단순한 규칙**을 고릅니다. 못 내면
   학습 실험으로 가지 않습니다.
2. 순진한 파리 `|PI_G| < 0.1` (시각 가치 0이라 구성상 0에 가까워야 함 — 배치 버그 검출용).

**✅ 완료.**
- **바닥**: 20 mm 타일 12×12(`flyplay/arena.py` `ColourFloor`). 90초 시험에 초파리가 1 m 넘게
  걸으므로 타일 격자를 **체커 한 주기(2칸)씩** 초파리 밑으로 옮깁니다 — 무늬는 모든 위치에서 그대로라
  초파리는 모르고, 어느 색 위인지는 무늬에서 계산합니다. 격자를 24×24로 늘려도 하부 시야 낱눈
  736개 중 바뀌는 것 0개(가장자리가 안 보임), 렌더 비용 +8 ms. `10_sense_check.py`에 바닥 색 3항목
  추가, **10/10**.
- **천장 시험**(가치 −0.5로 설정한 파리, 30초 시험, 조건당 8마리, LI 음수 = CS+ 회피):

| 조향 | LI |
|---|---:|
| 색 조향 없음 | +0.00 ± 0.01 |
| **좌우 비교, 이득 3 (채택)** | **−0.56 ± 0.05** |
| 좌우 비교, 이득 10 | −0.74 ± 0.06 |
| 속도 조절만 | +0.02 ± 0.06 |
| 좌우 비교 3 + 속도 조절 | −0.32 ± 0.05 |
| 경계 급회전만 | −0.18 ± 0.03 |
| 좌우 비교 3 + 경계 급회전 | −0.61 ± 0.04 |

  좌우 비교가 거의 전부입니다. 속도 조절은 빨라지는 만큼 회전 여유를 빼앗아 오히려 손해이고,
  경계 급회전은 오차 범위 안에서만 더합니다. 이득 3은 두 눈의 가치 차이 0.33에서 회전이 포화하므로
  기억 깊이에 따라 회피 강도가 달라집니다(10은 거의 켜짐/꺼짐).

## A-4. 실험과 분석 (`scripts/14_run_conditioning.py`, `scripts/15_analyze_mb.py`)

`--experiment colour` (Vogt 2014), `--experiment brightness` (Vogt 2016). 조건마다 G+/B+ 각
5마리.

| 실험 | 조건 | 기대(문헌) |
|---|---|---|
| colour | 처벌 기준 | LI 음수 |
| colour | 보상 기준 | LI 양수 |
| colour | 가소성 없음 | LI ≈ 0 |
| colour | 전역 도파민 | 가치 분리 정확히 0 |
| colour | 시각 KC 차단 | 색 LI ≈ 0, 같은 파리의 냄새 기억은 그대로 (Vogt 2016 γd) |
| colour | 처벌 DAN 차단 / 보상 DAN 차단 | 해당 기억만 사라짐 (PPL1/PAM) |
| brightness | 색 과제 × {기준, 색 VPN 차단, 밝기 VPN 차단} | 색 VPN 차단에서만 사라짐 |
| brightness | 밝기 과제(초록 10:1) × 같은 3조건 | 밝기 VPN 차단에서만 사라짐 |

모델 예측으로 따로 기록(문헌에 맞추지 않음): 훈련 횟수 1–4회 곡선(Vogt: 1회로 이미 유의),
도파민 진폭 곡선(Vogt: 전압에 대해 포화).

**비용(측정).** 파리 한 마리 = 훈련 480 s + 시험 90 s(시뮬). 색 시행은 단독 1.2–2.1 wall s/시뮬 s,
**15마리 동시에는 마리당 3.7**(10초 시행으로 측정 — 냄새의 약 1.7보다 병렬 효율이 나쁘고, 눈 렌더가
내장 GPU 하나를 나눠 쓰는 것이 유력하지만 확인 안 함).

**계획에서 바꾼 것 — 동일성 조건은 2마리.** 전역 도파민·γd 차단·처벌/보상 DAN 차단은 두 눈의 학습된
가치를 **정확히 0**으로 만들므로 조향도 가소성 없는 파리와 같고, 같은 시드면 경로가 끝자리까지
같아야 합니다. 노이즈 섞인 10마리보다 "경로 동일" 확인이 강한 증거라 2마리로 돌리고 요약표가
경로 일치를 검사합니다. 밝기 실험의 색 과제 기준선은 colour 실험의 `colour` 조건을 그대로 씁니다.
반대로 VPN 차단 조건들은 정확한 0이 아닙니다 — 렌더한 파랑·초록의 밝기가 소수 셋째 자리까지만
같고, 1/10 초록은 약간 탈색돼(대립값 0.84 대 0.90) 색 경로로도 조금 구별될 수 있어 전부 10마리.

**🔄 진행 중.** colour 스윕 11:39 시작(7조건, 38마리, 예상 약 1시간 45분). 그다음 brightness(5조건,
50마리, 약 2시간). 결과는 여기와 RESEARCH.md 13절에 기록합니다.

**분석.** 15번에 색 실험용 표·그림: 조건별 LI 막대(Vogt 그림 형식), 시험 90 s 동안의 선호
시간 곡선(Vogt: 약 20 s에 안정), 구획별 시각/후각 가중치, VPN 이중 해리 2×3 막대.

## A-5. 뷰어와 기억 (`scripts/09_web_viewer.py`, `scripts/16_memory.py`)

사용자 요청("시각에 대해서도 그래프")을 여기서 채웁니다.

- `--conditioning --modality colour`: 훈련/시험 블록이 Vogt 프로토콜을 따름
- **색 눈 모자이크** — 노랑형은 초록, 옅은형은 파랑으로 칠한 좌우 겹눈(지금은 회색 max)
- **VPN 막대** — 눈별 색 VPN(파랑쪽/초록쪽)과 밝기 VPN
- **시각 KC 래스터** — 100칸, 파랑·초록 기준 배치를 배경으로
- **눈별 시각 가치 선** — 왼눈/오른눈과 그 차이(조향 명령)
- **점유 막대** — 시험 중 CS+/CS− 타일 위에 있던 시간 누적
- 그래프마다 전체 설명(말줄임 없음), 실험 설명 스트립에 블록별 "지금 볼 것"
- 기억: `curate`에 `blue-punished`, `green-rewarded`, `gd-blocked` 추가, `--memory`로 불러오기

**✅ 뷰어 완료** (기억 정리는 스윕 후). `09_web_viewer.py --conditioning --modality colour`로 순진 시험 1 →
훈련 8(CS+ 4, CS− 4) → 시험 1. 색 겹눈 모자이크, 눈별 VPN 막대(색 24 + 밝기 4), 두 눈의 시각
KC 래스터(파랑·초록 기준 배치 위), MBON(시각 단위), 색별 가치(CS+/CS−), 두 눈 가치와 회전(최근
10초), 헤더에 발밑 색과 시험 중 색별 체류 시간·진행 중 선호도. 브라우저에서 확인(콘솔 오류 0).
사용자 요청으로 **세로 드래그 방향 반전**(아래로 끌면 카메라가 위로).

## A 완료 기준과 보고

- `13_mb_check.py` 기존 16 + 시각 항목 전부 통과, `10_sense_check.py`에 색 항목 추가 통과
- 천장 LI ≤ −0.3, 처벌 기준 LI가 가소성 없음 대비 뚜렷이 음수(평균 ± 표준오차 보고)
- 위 표의 대조군이 문헌과 같은 방향인지 표로 보고 — 다르면 그 자체를 결과로 기록
- RESEARCH.md 13절에 결과, README §6, STATUS, CLAUDE.md 불변식 갱신
- **여기서 사용자에게 결과를 보고하고 B로 넘어갑니다.**

---

# B. 밀폐된 방에서 냄새로 먹이 찾기

## B-0. 몸이 방을 견디는가 (`flyplay/room.py`)

오늘 측정으로 생긴 관문입니다. 여기를 못 넘으면 뒤의 어떤 전략도 평가할 수 없습니다.

- **방.** 80 × 80 mm, 벽 높이 8 mm, 장애물은 상자·원기둥. 전부 `add_fly` 전에 `ground_geoms`로
  (충돌은 안전망). `leg_contacts`는 바닥 geom만 세도록 고치고, `touching_pillar`를 모든
  벽·장애물로 일반화.
- **근접 반사.** 실제 초파리는 더듬이·앞다리로 닿아서 알지만 이 몸은 닿으면 뒤집히므로,
  머리 앞 부채꼴 안의 장애물까지 거리로 "닿기 직전"을 대신합니다(대용물임을 명시). 선회 반경
  4.7 mm를 감안해 감지 거리·감속을 측정으로 정합니다. 가까워지면 느려지고(대칭 신호로 속도
  조절 가능, 측정) 먼 쪽으로 돕니다. 벽을 한쪽에 두고 일정 거리로 따라가는 모드 포함.
- **오라클.** 장애물을 몸 폭만큼 부풀린 격자에서 측지 거리 장을 구해 그 기울기로 조향. 정답을
  아는 기준선이라 **정책의 입력으로는 절대 쓰지 않습니다.**

**통과 기준** (`scripts/18_room_check.py`):
- 무작위 방향으로 벽에 100회 접근 → **뒤집힘 0회**, 충돌 접촉 시간 기록
- 오라클 + 반사로 무작위 배치 20개(오목한 함정 포함) → 60 s 안에 **90% 이상** 도달
- 장애물 뒤로 도는 데 필요한 최소 통로 폭 측정(선회 반경 기준 약 10 mm 예상)

## B-1. 냄새 물리 (`flyplay/odor_grid.py`)

`GridOdorField` — `OdorField`와 같은 인터페이스(`sources`, `bind`, `read(sim) → (4, n_dim)`,
`left_right`, `asymmetry`)라서 조건화·뷰어·전략 코드가 그대로 받습니다. 센서 위치에서 격자를
쌍선형 보간합니다.

| 조건 | 식 | 비고 |
|---|---|---|
| 정지 공기 | `D∇²c − k c + S = 0` | 확산만으로 80 mm에 약 9분(L²/D) → 발생원이 오래 있던 방의 정상상태로 봄 |
| 바람 | `u·∇c = D_eff∇²c − k c + S` | `u`는 장애물을 돌아가는 퍼텐셜 흐름(라플라스), 유입·유출 벽. 난류 없음 |

- 벽·장애물은 무유속 경계, 흡수는 `k = D/ℓ²`로 두고 `ℓ` = 15·30·60 mm를 모두 돌려 민감도 보고.
  **흡수원이 없으면 정상상태가 없습니다.**
- `D` = 12.4 mm²/s(아세트산, Tang 2015; 에틸아세테이트는 8.6). 방출량은 빈 공간에서 3 mm 거리
  농도가 0.111(KC 작동 범위 상한)이 되게 맞춥니다.
- 80 mm, 격자 0.5 mm → 약 2.6만 미지수, scipy 희소 직접 풀이(몇 초 예상, 측정).
- **감지 잡음**을 `read`에 선택 항목으로 추가. 잡음이 없으면 지수적으로 작은 기울기도 따라갈 수 있어
  비현실적입니다.

**통과 기준** (`scripts/19_odor_grid_check.py`):
- 벽에서 먼 빈 방: 해석해 `K0(r/ℓ)`와 3–25 mm에서 상대오차 < 5%
- 질량 보존: 방출 = 흡수 적분 ± 1%
- 벽 법선 방향 기울기 ≈ 0
- 극값: 발생원 외 자유 공간에 극대 없음 → **잡음 없는 기울기 상승은 모든 출발점에서 도달**
  (최대원리의 확인, 감지자 상한)
- 감지 한계 지도: 더듬이 간격에서 좌우차가 잡음보다 작은 영역 비율을 잡음 수준별로 기록
- 비교: 같은 배치에서 지금의 직선거리 I/d² 모델이 벽 너머를 가리키는 사례 수

## B-2. 실제 초파리 전략 (`flyplay/strategies.py`)

전략마다 문헌의 식을 그대로 옮기고, 이 몸에 맞춰야 하는 부분만 명시적으로 바꿉니다.

| 전략 | 식과 값 | 이 몸에 옮길 때 |
|---|---|---|
| **ON/OFF 모델** (Álvarez-Salvado 2018, 저자 코드) | 적응 τ 9.8 s → `C = c/(c+0.01+A)` → ON τ 0.72 s, OFF = 저역통과 4.84 s − 0.62 s. 속도 `v₀(1 + 0.45·ON − 0.8·OFF)`, 회전 확률 `0.12 − 0.03·ON + 0.75·OFF`/20 ms, 회전 크기 `20·randn²`°, 바람 거슬러 `−5·ON·sin θ`°/샘플 | 농도는 최댓값 1로 정규화. 속도는 대칭 하행 신호로(측정: 신호 × 18.6 mm/s), v₀는 이 몸의 순항 속도로 비례 환산. 회전은 [0.4, 1.6] 최대 선회(228°/s)를 각도만큼의 시간 동안 |
| **만남 기반 회전·정지** (Demir 2020; NeuroMechFly v2 값) | 회전 시점 포아송(1.33/s), 방향 `P(바람 거슬러) = 1/(1+e^(−α·W))`, W는 만남의 지수 가중합(τ 2 s). 걷기→정지 `0.78 − 0.8·e^(−Δt/0.2)`, 정지→걷기 `0.5 + 1·∫e^(−Δt/0.52)` | 1.x의 회전 명령 (−0.4, 1.2)은 우리 Walker에서 방향이 뒤집히므로 [0.4, 1.6]으로. 정지는 대칭 0(측정: 0.5 mm/s). 정지 후 재출발은 B-0에서 측정 |
| 좌우 비교 (약한 채널) | `κ₇·(C_L − C_R)`; 효과 작음(40°/s에서 무의미, 300°/s에서 67→76%) | 기존 조향 법칙 재사용 |
| 정지 공기 | 경계에서 감속 + 안쪽 급회전 (Tao 2020) | ON/OFF 모델에서 바람 항만 끔 |
| 국소 탐색 | 설탕 접촉이 촉발(Corfas 2019). 적분기를 먹이에서 0으로 맞추고 왕복 거리를 늘림(Behbahani 2021) | B-3의 경로 적분 사용 |
| 벽 | 문헌은 "경계를 보고 머묾"뿐, 회피 규칙 없음(Soibam 2012) | B-0의 근접 반사 — **문헌이 아니라 이 몸의 제약**임을 명시 |

중재는 우선순위 하나로: 근접 반사 > 국소 탐색(먹이 뒤) > ON/OFF 또는 만남 모델 > 좌우 비교.

**통과 기준** (`scripts/20_strategy_check.py`, 장애물 없는 방):
- 냄새 켜짐/꺼짐 펄스에서 이 몸이 논문의 서명을 재현 — 켜지면 가속·바람 쪽 회전, 꺼지면 감속·회전 증가
- 바람 + ON/OFF: 오라클 대비 도달률 기록. 논문 수치(난류 플룸에서 실제 56%, 모델 66%)는
  플룸 종류가 달라 기준이 아니라 참고로만 표기
- 바람 없음 + 좌우 비교만: **못 찾는 것이 문헌의 예측**(Álvarez-Salvado) — 반대로 잘 찾으면
  냄새장이나 잡음 모델을 의심

## B-3. 중심복합체형 나침반과 목표 (`flyplay/central_complex.py`)

| 부품 | 모델 | 근거 |
|---|---|---|
| 머리 방향(EPG) | 8칸 고리 활동 덩어리. 회전 속도 적분 + 방 벽의 시각 지표로 보정, 지표→고리 연결은 동시 활성 시 약화되는 가소성 | Seelig & Jayaraman 2015(어둠에서 30 s 이상 유지하며 표류, 폭 82°), Fisher 2019(수 분 안에 재배열) |
| 경로 적분 | 머리 방향 × 이동 속도. 속도는 1.x의 보폭 회귀(R² 0.96/0.97) 또는 참값 + 잡음 | Behbahani 2021, FlyGym 1.x 예제 |
| 목표(FC2) | 방향 위상 하나. 갱신: 바람 + 냄새 ON → 바람 거슬러 방향, 냄새 가장자리를 건너면 건넌 방향과 선형 혼합, 먹이에서 경로 적분 벡터. 버섯체 가치가 크기·부호를 조절 | Mussells Pires 2024(FC2), Matheson 2022(냄새→FB5AB→hΔC), Siliciano 2026(선형 혼합), Li 2020(MBON→부채꼴체 해부) |
| 조향(PFL3) | 좌우 각 8개 `r = softplus(cos(H − H_i ± 67.5°) + d·cos(G − G_i))`, 회전 = 오른쪽 합 − 왼쪽 합. PFL2로 반대 방향 근처 이득 증가 | Westeinde 2024, Mussells Pires 2024. `d`는 논문에 값 없음 → 측정으로 정함 |
| 우회 기억 | 반사가 조향을 가로채는 동안 목표 유지, 시간상수로 감쇠. 문헌이 "수 초"(Neuser 2008)부터 약 24 s(리뷰)까지라 2–30 s를 쓸어 봄 | Kuntz 2012 과제 |

**통과 기준** (`scripts/21_cx_check.py`):
- 잡음 없는 덩어리에서 PFL3 출력이 `sin(G − H)` 모양이고 목표에서 0 (항등식)
- 머리 방향 표류: 회전 적분만 vs 지표 보정, 60 s
- 우회 시험(Kuntz 2012형): 목표 설정 → 반사로 90° 강제 우회 3 s → 원래 방향 복귀율을 우연 수준과 비교

## B-4. 방 실험 (`scripts/22_run_room.py`, `scripts/23_analyze_room.py`)

- **방**: 80 × 80 mm, 먹이 = 설탕 방울(접촉 시 발견 + 국소 탐색 촉발) + 같은 자리의 휘발성
  냄새(아세트산, D 0.124 cm²/s). 배치 20개(통로 ≥ 선회 반경 기준 폭) + 오목한 함정 5개.
- **조건**(전부 조합하지 않고 질문별로):

| 질문 | 비교 |
|---|---|
| 냄새 물리가 중요한가 | 같은 전략에서 I/d²(벽 통과) vs 정지 공기 격자장 |
| 바람이 필요한가 | 정지 공기 vs 바람 (ON/OFF 모델) |
| 목표 기억이 필요한가 | 전략만 vs + 중심복합체 목표 / 오목한 함정 배치에서 |
| 얼마나 잘하나 | 모든 조건 vs 오라클 |
| 배운 가치로 고르나 | 발생원 2개(A = 설탕과 짝, B = 맹물), 보상 도파민으로 A에 양의 가치 |

- **지표**: 120 s 안의 도달률, 도달 시간, 경로 길이 ÷ 측지 최단거리, 갇힌 시간(10 s 이상
  진전 없음), 근접 반사 발동 수, 뒤집힘(0이어야 함).
- **비용(추정)**: 시각 10 Hz면 120 s 시행이 병렬에서 약 5분. 25배치 × 질문별 조건 × 3시드면
  수 시간 → 개발은 배치 5개, 최종만 전체.
- 선택: A에서 배운 **색 표지**를 먹이 옆에 두어 버섯체 시각이 방 실험에 들어가는 조건 — A·B가
  모두 끝난 뒤.
- 결과는 **모델의 예측**으로 보고합니다. 걷는 곤충이 장애물을 돌아 냄새를 따라간 실험 자료를
  찾지 못했기 때문입니다.

---

# C. 샌드박스 방 — 설탕·전기 자극을 직접 놓고 보기 (2026-09-17 사용자 요청)

사용자 요청: "다양한 종류의 설탕과 전기 자극을 사각 밀폐 맵에 임의로 부여하면서 초파리가 어떻게
반응하는지", **뷰어에서 실시간으로 환경을 조종**하면서. 배경 스윕은 하지 않고 보이는 것을 먼저
만듭니다. B-0(벽 반사)을 이 안에서 끝내고, B-1~B-3은 샌드박스를 더 사실적으로 만드는 후속
단계가 됩니다.

## 자극의 정의 — 감각 · 도파민 · 타고난 반응

| 자극 | 초파리가 느끼는 것 | 도파민(학습) | 타고난 반응 | 근거 |
|---|---|---|---|---|
| **설탕** (자당·과당·아라비노스·소르비톨·아라비노스+소르비톨, 농도) | 디딘 발끝이 방울 안 | **단맛 채널** → 빠르게 배우고 몇 시간에 잊는 구획(배불러도 기록). **영양 채널** → 오래 가는 구획(배고플 때만 기록) | 배고프고 단맛이 나면 **멈춰 먹음**, 영양이 있으면 배고픔이 줄어듦. 먹은 뒤 **주변 탐색** | Burke & Waddell 2011, Huetteroth 2015, Yamagata 2015, Thoma 2016, Corfas 2019 |
| **전기** (전압, 크기) | 디딘 발끝이 구역 안 | 처벌 → **빠른 구획**(γ1pedc형: 한 번에 배우고 수 시간에 잊음) + **느린 구획**(α3형: 천천히 배우고 며칠 감) | 돌아서 달아남. 배고픔×단맛이 크면 약한 전기는 견디고 먹음 | Aso & Rubin 2016, Aso 2012, Vogt 2014(15 V 유의, 30–120 V 포화), Chadha & Cook 2014(운동 반응 중간점 45 V) |
| **냄새** (식초·옥탄올·MCH, 세기) | 더듬이 농도 | 그 순간의 KC 부호가 연합됨 | 식초만 타고난 끌림(배고픔 비례) | 기존 후각 버섯체 |
| **색 바닥** (파랑·초록, 크기) | 겹눈 | 그 순간의 시각 KC 부호가 연합됨 | 없음 | A절 |
| **장애물·벽** | 앞쪽 거리 | — | 닿기 전 회피(몸의 제약) | B-0 측정 |

**배고픔은 표현을 조절합니다** — 먹이 기억은 배고픈 만큼만 행동으로 나오고 전기 기억은 늘 나옵니다
(Krashes 2009: 먹고 10–30분이면 기억이 가려지고 다시 굶기면 돌아옴). 배고픔 시간 척도(원래 수 시간)와
포만 속도는 보기 좋게 **압축**했고 그렇다고 화면에 표시합니다.

**배울 수 없는 것(정직하게 화면에 적음)**: 단서 없는 **장소** 기억(Ofstad 2011·heat-box — 버섯체가 아니라
중심복합체), 장애물 뒤의 냄새(지금은 직선거리 냄새, B-1에서 교체).

## 단계

| 단계 | 내용 | 상태 |
|---|---|---|
| C-0 | 방·벽·물건 풀(재빌드 없이 배치), 근접 반사 | ✅ 벽 접근 12회 × 12초: 뒤집힘 0, 접촉 0 |
| C-1 | 자극 감지(발끝·더듬이·겹눈)와 타고난 반응(먹기·도망·탐색), 어두운 방바닥 | ✅ (측정: 밝은 회색 바닥이면 파랑이 설탕 가치를 +0.66 물려받음 → 어두운 바닥에서 0.00) |
| C-2 | 뷰어 샌드박스 모드: 지도 편집(클릭·끌기·오른쪽 클릭·선택 후 슬라이더), 프리셋, 배고픔, 새 파리, 배운 가치·도파민·사건 기록 | ✅ 브라우저에서 배치·이동·수정·삭제 확인 |
| C-3 | 문헌 반영: 설탕 두 채널(단맛·영양), 처벌 빠른/느린 구획, 영양 기록은 배고플 때만, 전기 1.5초 펄스, 견딤 규칙(배고픔×먹이 끌림 > 전기 도주 구동), 탐색 길이 | ✅ 파랑 전기 방에서 90초 동안 파랑 0.6% 대 초록 15% 체류(면적 각 6.8%) |
| C-3b | 사용자 요청 반영: 설탕이 먹는 만큼 줄고 사라짐(초당 5 nl, flyPAD) + **켜고 끄는 버튼**, 전기판 투명(지도에만 점선), 방울 크기 = 양(버그 수정: 양을 늘리면 작아지던 것), **오른쪽 드래그로 화면·지도 이동**, 휠 확대, 더블클릭 원위치. 후속 수정: 맛보는 반경이 그려진 크기를 따르게 함(최소 1.5 mm) — 고정 2.5 mm일 때 20·100·500 nl 방울의 행동이 한 스텝도 다르지 않았음 | ✅ 브라우저에서 확인. 수정 후 90초: 20 nl는 2.7초에 찾아 가슴이 중심에서 0.5~2.1 mm인 채로 먹고, 500 nl(반경 5.6 mm)는 가장자리(가슴 ~5 mm)에서 먹기 시작, 100 nl는 이전과 동일 |
| C-4 | 프리셋 시나리오를 돌려 보고 결과를 짧게 보고 | ✅ 식초 설탕: 2.5초에 찾아 먹기 시작, 100 nl를 87초에 다 먹음. 파란 바닥 전기: 파랑 0.6% 대 초록 15% 체류. 갈등: 30 V를 처음엔 견디지만 전기와 함께 맡은 식초가 싫어져(−0.41) 설탕에 닿기 전에 포기. 장애물 너머: 벽을 따라 걷다 끝(y≈29)을 돌아 5.4초에 먹기 시작(뚫고 지나간 것 아님, 장애물 접촉도 물리적으로 잡힘). 단맛 vs 영양: 처음 배치(아라비노스 vs 소르비톨 단독, 멀리)는 120초 동안 아무것도 못 찾아 **아라비노스 vs 아라비노스+소르비톨**로 바꿈(Burke & Waddell 2011의 실제 비교) — 시드 4개×150초: 1.5~67초에 첫 방울, 혼합물은 배고픔 0.80→0.25~0.56, 아라비노스만은 0.88까지 상승 |
| C-5 | 사용자 질문 반영: **주둥이 뻗기(PER)**, 파리 색, 배고픔–가치 관계 문헌 대조 후 수정 | ✅ 주둥이: 샌드박스 파리에만 rostrum·haustellum 관절+모터(스프링 5, 감쇠 0.5). 처음 자세 (−1.40, +1.80)는 사용자가 어색하다고 함(47° 꺾여 끝이 뒤를 향함) → 일직선 자세 (−1.9, +2.7): 두 마디 69–70° 앞·아래, 꺾임 0.7°, 끝이 설탕 표면 높이(0.05 mm)에 닿음. 시점도 고침: 멈춘 뒤 뻗기(시작 시 속도 중앙값 0.34 mm/s), 한 모금 리듬(접촉 0.145초·떨어짐 0.075초, Itskov 2014; 끝이 0.27 rad 들썩임), 떠나기 전 0.3초 멈춰 접기, 식초 표시 공을 머리 높이 위(3.5 mm)로. 90초: 10번 먹기, 방울 다 먹음, '설탕에서 벗어남' 0번(이전 3번). 눈 입력 차이 0(숨김 부위), 걷기 13.84 대 13.85 mm/s·회전 147.5 대 147.8 °/s. 눈 입력 차이 0(숨김 부위), 걷기 13.84 대 13.85 mm/s·회전 147.5 대 147.8 °/s. 색: 모델에 색을 넣으면 파리가 자기 다리 색을 봄(낱눈 350/1442 변화, 시각 KC 코드 57% 샘플에서 달라짐) → 모델은 무색, 뷰어 화면에서만 칠함. 배고픔: 단맛 보상 × 배고픔(Burke 2012), 느린 처벌 × (1−배고픔)(Plaçais & Preat 2013) — 측정: 느린/빠른 비 0.080(배고픔 0.2) 대 0.010(0.9), 먹은 1초당 단맛 학습 2.96배(0.9 대 0.3). **남은 문제**: A의 색 조건화 실험은 색 있는 파리로 돌아 자기 다리 색이 시각 코드에 섞였을 수 있음(재측정 안 함) |

---

## 순서와 보고 시점

| 순서 | 단계 | 끝나면 |
|---|---|---|
| 1 | A-1 → A-2 → A-3 천장 시험 | 천장 LI를 보고 조향 규칙 확정 |
| 2 | A-4 → A-5 | **A 결과 보고** (표·그림·뷰어) |
| 3 | **C (샌드박스)** — 사용자 요청으로 B보다 먼저 | 뷰어에서 직접 조종 |
| 4 | B-1 | 냄새장 지도 보고 |
| 5 | B-2 → B-3 → B-4 | 최종 보고 |

## 하지 않을 것

- 난류 플룸(phiflow 없음, 시간에 따라 변하는 장) — 정상상태 장으로 먼저 끝냄
- UV·R1–6 채널 추가(FlyGym 망막 수정)
- Okray 2023의 복합 감각 결합 기작(철회됨)
- α/βp형 상부 시야 시각 KC — B-3 지표 학습에서 필요해지면
- 커넥톰으로 제약한 중심복합체 배선
- PPO 재학습

## 주의할 실패 모드

- **시각을 켠 채 냄새 실험을 돌리면 바닥이 맥락으로 학습됩니다.** 처벌 시행마다 바닥을 보는 시각
  KC도 눌립니다. 냄새 실험은 시각을 끈 채 비트 단위로 같아야 합니다.
- **눈 렌더러는 시뮬레이션 스레드에서 만들어야 합니다**(뷰어). 색 모자이크가 어두우면 이것부터 의심.
- **상부 시야 수평선 보정은 방에서 깨집니다.** 벽이 늘 보이므로 기존 `RetinaFeatures`를 방에서 쓰지
  않습니다.
- **벽 접촉 = 뒤집힘.** 반사가 늦으면 전략 비교가 아니라 넘어지는 빈도를 재게 됩니다.
- **오라클 장이 정책 입력으로 새면** 결과 전체가 무의미합니다.
- **비대칭 하행 신호 0.4 미만은 회전 방향이 뒤집힙니다.** 1.x 제어기의 (−0.4, 1.2)를 옮길 때 주의.
  대칭 0은 정지로 측정됐지만 정지 후 재출발은 아직 안 잼.

## 만들 파일 / 고칠 파일

```
flyplay/visual_pathway.py        광수용기 → VPN → γd형 시각 KC            (A)
flyplay/room.py                  방·벽·장애물, 근접 반사, 오라클          (B-0)
flyplay/odor_grid.py             정상상태 이류–확산 냄새장                (B-1)
flyplay/strategies.py            전략 상태기계                            (B-2)
flyplay/central_complex.py       머리 방향·목표·PFL3                      (B-3)
scripts/18_room_check.py         B-0 pass/fail
scripts/19_odor_grid_check.py    B-1 pass/fail
scripts/20_strategy_check.py     B-2 pass/fail
scripts/21_cx_check.py           B-3 pass/fail
scripts/22_run_room.py, 23_analyze_room.py                                (B-4)
```

고칠 파일: `flyplay/mushroom_body.py`, `flyplay/conditioning.py`, `flyplay/arena.py`,
`flyplay/build.py`, `scripts/09_web_viewer.py`, `scripts/10_sense_check.py`,
`scripts/13_mb_check.py`, `scripts/14_run_conditioning.py`, `scripts/15_analyze_mb.py`,
`scripts/16_memory.py`, 문서 4개.

## 검증

```bash
.venv\Scripts\python.exe scripts\13_mb_check.py
```

기존 16항목 + 시각 항목. 몇 초.

```bash
.venv\Scripts\python.exe scripts\10_sense_check.py
```

기존 7항목 + 바닥 색 좌우 신호, 실행 중 색 변경.

```bash
.venv\Scripts\python.exe scripts\14_run_conditioning.py --experiment colour
```

```bash
.venv\Scripts\python.exe scripts\15_analyze_mb.py --dir out\mb_colour
```

```bash
.venv\Scripts\python.exe scripts\09_web_viewer.py --conditioning --modality colour --port 8010
```

B 단계 검증 명령은 B-2·B-3 확정 후 추가합니다.

---

# D. 중학생 페르소나 사용성 테스트 (2026-09-17)

방학 과학 자유탐구에 쓰려는 중2 역할의 에이전트가 브라우저로만 1366×768에서 약 70분 사용
(주둥이·색·배고픔 수정 **전** 버전). 실제로 "배고픔 0.80 대 0.20, 각 3회, 60초" 실험을 해서 평균
먹은 시간 18.2초 대 4.9초를 얻음. 총평: 60초마다 직접 멈추고 공책에 받아 적느라 힘들다 —
"60초 실험 시작"과 "결과표 저장" 버튼이 있으면 좋겠다.

| 우선 | 항목 | 종류 |
|---|---|---|
| 1 | 실험 모드: N초 자동 멈춤, 결과 한 줄씩 표에 쌓기 + CSV, 시작 때 방·파리 함께 초기화(먹은 설탕 복원), 시작 방향 무작위 | 기능 없음 |
| 2 | 일시정지 중 새 파리 → 배고픔 0.00 표시(`reset_fly`가 `Telemetry()`로 갈아끼우고 멈춘 동안 갱신 없음) | 버그 |
| 2 | 전기 한 번 밟아도 여러 회로 셈(딛는 다리 기준이라 발을 떼고 디딜 때마다 새 접촉) | 버그 |
| 2 | 샌드박스에서 "먹이까지"는 늘 "-"(다른 모드용 칸) | 버그 |
| 2 | 고른 물건이 있으면 도구 옵션 변경이 그 물건에 적용되고 선택 해제 방법 없음 | UX 함정 |
| 2 | 일시정지 중 프리셋을 바꿔도 3D 화면 갱신 안 됨 | 버그 |
| 2 | 속도 버튼은 1.0x인데 실제 0.39배(8010 단독 측정, 12초) | 표시 오해 |
| 3 | 노트북 화면에서 방 지도 한 변 약 60px, 이름표 겹침, 크기 슬라이더 숫자 없음, 머리줄 오른쪽 잘림 | 배치 |
| 3 | 용어 설명 없음(하행 L/R, 기울기, 접지, M, nl, 가치, 버섯체, 도파민) | 쉬운 설명 |
| 4 | "피한다" 지표(파랑 위 체류 비율), 사진 저장, 삭제 기록·되돌리기 | 기능 없음 |
| - | 냄새 없는 설탕은 우연히만 찾음, 100 nl는 20초면 다 먹어 배고픈 조건이 막힘, 프리셋만 바꾸면 학습이 남음 | 모델 동작 — 안내 필요 |

**진행 (2026-09-17, 사용자 "ㅇㅇ" + 추가 요청)** — 전부 브라우저(1366×768)와 HTTP로 확인:

- ✅ 실험 모드: 영상 아래 [설명 | 실험 모드] 탭. 조건 이름·방(지금 방/프리셋)·시작 배고픔·시간(30초~10분)·새 파리·시작 방향 무작위 → 정해진 시간에 정확히 멈춤(10.00초, 30.0초 측정), 결과표에 24칸 한 줄(처음 먹기·먹은 횟수/시간·설탕별·끝 배고픔·전기 횟수·파랑/초록 위 %·이동 거리·남은 설탕·냄새/색 가치), CSV 내려받기 + `out/sandbox/trials.csv`에 자동 누적. 같은 방으로 반복하면 먹은 설탕이 다시 채워짐.
- ✅ 버그: 일시정지 중 새 파리 배고픔 0.00(→0.80), 전기 중복 세기(1초 재무장), 샌드박스 머리줄 값(가장 가까운 설탕까지·냄새 합계·벽 접촉, 쉬운 이름), 선택 함정(아래), 일시정지 중 화면 갱신(`mj_forward`), 실제 속도 표시(×0.3~0.45 측정).
- ✅ 긴 벽: 장애물 모양(블록/가로 벽/세로 벽)과 길이. 충돌 경계값 갱신을 측정으로 확인(위 CLAUDE.md).
- ✅ 프리셋 16개, 4묶음(기본·설탕 비교·학습·벽과 미로): 식초 없는 같은 자리 설탕, 작은 설탕 여러 개, 냄새 두 개(좋아하기 학습), 냄새+전기(피하기 학습), 초록 바닥 설탕(색 좋아하기), 설탕 두 개 중 하나만 전기, 벽 너머, 통로, T자 미로, 지그재그 미로, 기둥 숲. 16개 × 90초 점검: 뒤집힘 0, 벽·미로 전부 설탕 도달(3~70초).
- ✅ 사용자 추가 요청: **선택 도구**(놓기 도구는 겹쳐도 항상 새로 놓음, 선택 도구에서만 고르기·끌기, 같은 자리 다시 클릭하면 겹친 다음 물건, 고른 물건은 바꾼 값만 반영, Esc 해제), **지도 이름표 간단히/자세히/없음 + 마우스 올리면 정보 상자**, **지나온 길 남는 시간 슬라이더(0~180초)**, **3D 화면 전기판 빨간 점선**(뷰어 장면에만 그림 — 파리 눈과 무관).
- ✅ 배치: 1366×768에서 지도 캔버스 60 px → 282 px(학습 막대를 아래 줄로, 지도 설명은 "사용법 ⓘ"로).
- ✅ 되돌리기/다시 하기(사용자 요청): 도구 줄 ↶/↷ 버튼, Ctrl+Z / Ctrl+Y. 놓기·옮기기·값 바꾸기·지우기·전부 지우기·프리셋을 편집 하나씩 거꾸로 적용(먹은 설탕은 되살리지 않음), 끌기·슬라이더 한 번 = 한 단계, 지운 물건은 같은 번호로 복귀. HTTP 12항목 통과 + 브라우저에서 실제 클릭·끌기·키로 확인.
- ✅ 초파리 가운데로(사용자 요청): 같은 파리를 방 가운데로 — 기억 가중치·배고픔·집계·시계 유지, 지나온 길은 새로 시작, 순간이동은 이동 거리에서 제외, 진행 중인 전기 도파민 펄스는 끊음. 9항목 통과(학습된 식초 0.457 유지, 이후 8초 84 mm 정상 보행) + 페이지 버튼 실제 클릭 확인. 이 과정에서 새 파리·실험 시작 때 속도 조절기 기준점을 다시 잡도록 고침.
- 남음(3·4순위): 쉬운 설명 모드 전체, 사진 저장, 속도 개선(물리가 실시간 한계).

---

# E. A/B 실험 세트 — 배경 반복 실행, 다시 보기, 탐구보고서 (2026-09-17)

사용자 요청은 한 대화에서 여러 번 이어졌습니다.
1. "프리셋을 더 다양하게, 실험 과제용 다른 자극 파악, 다른 사람들이 어떻게 했는지 확인. 여러 번 자동으로
   반복하면서 행동이 어떻게 바뀌는지 보고서를 쓸 수 있어야 함"
2. "눈으로 보며 학습을 확인하고 싶다 — 강화학습 때처럼 bg로 많이 돌리고 몇 개만 보여 주기" → "1보단 2"
3. "프리셋만 더 많았으면" → "프리셋은 A/B 한 세트여야 마지막에 가설이 통했는지 확인 가능"
4. "bg 실험은 GPU 활용" → "GPU가 더 빠를 것 같은데 확실해?"
5. "지나온 길 무한, 막대 길게" → "서버가 180초 이상 보내도록" → "히트맵 오버레이"
6. "초파리 위치도 방 편집에서 변경"

문헌 조사는 RESEARCH.md 14절, GPU 측정은 9.1절.

## 만든 것

| 부분 | 내용 | 확인 |
|---|---|---|
| 방 프리셋 | 16 → **50개**, 7묶음(기본·설탕 비교·냄새·학습 훈련 방·학습 시험 방·갈등과 장소·벽과 미로), 방마다 한 줄 설명. 풀 크기 확대(장애물 8→16, 설탕 8→12, 전기 6→8, 색 8→12, 냄새 6→8) | 풀 확대·측정 항목·초파리 옮기기 뒤에도 세 프리셋 10초 관절 기록 해시 동일. 풀 확대로 접촉 쌍 468→756, 스텝 23.0→23.2 ms |
| 시행 측정 | 26칸: 처음 설탕 닿기·처음 먹기·먹은 횟수/시간·끝 배고픔·남은 설탕·처음 전기·전기 횟수/시간·파랑/초록 위 %·**색·냄새 선호 지수(T자 미로처럼 더 가까운 쪽 점유)**·냄새별 10 mm 근처 %·이동 거리·평균 속도·멈춘 시간 %·다섯 가치 + 5 Hz 경로 | 스크립트 점검 |
| A/B 세트 | **22개**(먹이 찾기 5·배우기 10·갈등 3·미로 4) → 21개: "영양 없는 단맛도 배를 채울까"는 배고픔이 영양으로만 줄도록 짠 규칙을 확인할 뿐이라 뺐습니다(사용자 판단). 조건마다 한 가지 차이(전기 켬/끔, 설탕 있음/없음, 배고픔, 전압, 쉬는 시간, 같은 파리/새 파리, 벽 있음/없음…), 판정 측정·구간·예상 방향, 모델 작동 방식과 실제 연구 근거 | 시범 실행(아래) |
| 쌍둥이 파리 | A의 k번과 B의 k번이 시드(배선·탐색 난수)와 출발 방향을 공유 | 시범 실행에서 조작이 작용하기 전까지 두 조건의 시행 기록이 숫자까지 같음 |
| 배경 실행기 | `scripts/17_run_experiment.py` — 코어−2개 프로세스, 시행마다 `records/*.jsonl`에 추가, `STOP` 파일로 멈춤, 끝나면 CSV·보고서 | 명령줄·뷰어 둘 다 시작→진행→멈춤/끝 |
| 다시 보기 | 파리 1~3의 시행을 50 Hz 관절 기록으로 저장(시뮬레이션 1분에 0.85 MB), 뷰어 모델에 재생. 원래 파리는 `mj_getState`로 보관했다 복원 | 다른 시드로 기록해도 재생 위치 오차 0.006 mm, 복원 뒤 1초에 13.6 mm 정상 보행. 브라우저에서 재생·되감기·실시간 복귀 |
| 보고서 | 탐구보고서 순서(KOSAC 틀): 탐구 문제·동기·가설(학생이 고른 예상)·변인·방법(방 그림·순서·실제 연구)·결과(가설 판정, 쌍 점 그래프, **조건별 히트맵**, 시행별 곡선, 평균 표, 경로 그림, 원자료)·결론·한계·기록. 학생이 쓰는 칸은 브라우저에 자동 저장, HTML 저장·인쇄 | 브라우저 확인 |
| 판정 규칙 | 3쌍 이상, 평균 차 > 측정별 허용치, 쌍의 70% 이상이 같은 방향 → "맞았다". 부호 검정 확률을 "동전 던지기"로 함께 적고, 확률이 0.2 이상이면 "우연일 수도" 문장 | 1쌍 시범에서 "맞았다"가 나온 것을 고침 |
| 뷰어 | 처음에는 영상 아래 **A/B 실험 (배경)** 탭 한 칸에 세트·파리 수·예상·시작, A/B 방 보기, 기록 목록, 보고서, 멈추기, 따라 보기, 진행 표를 모두 두었다가 사용자가 어렵다고 해서 **A/B 실험실**(아래)로 옮김. 영상 위 다시 보기 막대. 3D 화면 최소 240 px | 1366×768에서 영상 39 px로 눌리던 것 → 240 px, 패널 192 px |
| 지나온 길·히트맵 | 서버가 하루치 경로를 float32로 보관, `/state`는 최신 50점, 오래된 점은 `/trail`에서 한 번(base64). 슬라이더 10초~1시간·무한, 히트맵(2 mm 칸, 제곱근 눈금, 기간은 지나온 길과 같음) | 새로고침 뒤 서버 232점과 페이지 232점 일치 |
| 초파리 옮기기 | "초파리" 도구로 클릭, 선택 도구로 끌기, 방향 선택, Ctrl+Z. 기억·배고픔·집계·시계 유지, 벽·장애물 3 mm 안 거부 | 7항목 통과 + 브라우저에서 클릭·끌기·되돌리기 |
| 속도 | numba(망막)·OpenBLAS의 쉬는 스레드가 잠들지 않던 것을 고침: 뷰어 프로세스 14.7코어 → 1.1코어, 실제 속도 ×0.35 → ×0.5(요청값) | 측정 |
| GPU | 걷는 파리 물리만 4096마리 12.8×, CPU 14프로세스 샌드박스 전체 6.5× → CPU 유지 | RESEARCH.md 9.1 |

## 시범 실행 — 조건마다 파리 3마리

사용자가 묻기 전에 22개 세트를 조건마다 3마리로 배경에서 돌렸다가, "실험이 왜 다 돌고 있지? 필요할 때
하면 되지 않나"는 말에 57분 만에 멈췄습니다(132개 작업 중 126개 끝남). 끝까지 간 세트의 판정:

| 세트 | 판정 측정 | A 평균 | B 평균 | 판정 (같은 방향 쌍) |
|---|---|---:|---:|---|
| 냄새가 먹이 찾기를 도울까 | 처음 먹기(초) | 3.2 | 60.0 (못 찾음) | 맞음 (3/3) |
| 배고플수록 더 오래 먹을까 | 먹은 시간(초) | 21.5 | 10.6 | 맞음 (3/3) |
| 맛이 없으면 안 먹을까 | 먹은 시간(초) | 21.3 | 0.0 | 맞음 (3/3) |
| 영양 없는 단맛도 배를 채울까 (나중에 삭제) | 끝 배고픔 | 0.20 | 0.98 | 맞음 (3/3) |
| 전기와 함께 맡은 냄새를 피할까 | 냄새 선호 | +0.07 | −0.04 | 평균만 가설 쪽 (2/3) |
| 설탕과 함께 맡은 냄새를 좋아할까 | 냄새 선호 | +0.45 | −0.04 | 평균만 가설 쪽 (2/3) |
| 거울 실험 | 냄새 선호 | +0.07 | −0.07 | 평균만 가설 쪽 (1/3) |
| 배가 부르면 설탕 기억을 안 쓸까 | 냄새 선호 | +0.45 | +0.38 | 차이 거의 없음 (1/3) |

- 먹이 찾기 세트는 3마리로도 뚜렷하고, 학습 세트는 시험 시행 하나하나의 흔들림이 커서 3마리로는
  방향만 보입니다. 학습 세트는 조건마다 8마리(기본값) 이상이 필요해 보입니다.
- 시범 실행 중 설계를 고친 세트 7개는 **새 설계로 돌려 보지 않았습니다**: 방울 크기(흩어진 방울 12개로),
  같은 파리 반복·미로 반복(식초 → 옥탄올), 색 학습 세 개(시험 방을 네 칸 바닥으로), 초록 섬 → "냄새
  단서가 있어야 전기를 미리 피할까"(`cue_needed`, 방 `cued_shocks`).
- 멈춘 뒤 보고서를 쓰다가 조건마다 시행 수가 다른 세트(`more_training`)에서 그래프가 IndexError로 죽는 것을
  찾아 고쳤습니다.
- 같은 시각 뷰어에서 한 세트가 1초 간격으로 두 번 시작돼(시작 버튼 연타) 코어 16개에 워커 40개가 돌았습니다.
  시작 버튼을 5초 잠그고, 서버도 같은 세트를 10초 안에 다시 시작하지 않게 했습니다.

## A/B 실험실 화면

사용자: "a/b 실험 사용방법이 너무 어려운데, ui 새로 짜봐". 옛 칸은 영상 아래 192 px에 새 실험 고르기와 지난 기록
고르기가 한 줄에 섞여 있었고(위에서 고른 세트와 아래 표의 실험이 서로 달랐음), 예상은 "A가 더 크다/작다"로만
고르게 되어 있어 A가 무엇이고 측정값이 어느 쪽으로 커지는지 스스로 풀어야 했습니다. 창 전체를 덮는 실험실로
옮기고 한 화면에 한 단계만 보이게 했습니다.

| 단계 | 화면 | 확인 |
|---|---|---|
| ① 질문 고르기 | 내 실험 카드(상태·진행 막대·판정), 질문 카드 21개(첫 방 그림, A·B 한 줄, 재는 것, 예상 시간, 해 본 횟수) | 1600×900·900×800 화면 캡처 |
| ② 가설 세우고 시작 | A·B 카드(방 순서 그림, 바뀐 값 강조, 같은 파리/새 파리, 배고픔), 변인(바꾸는 것·재는 것·같게 하는 것), 세트마다 쓴 가설 문장 두 개 + "비슷할 것이다"(고르기 전에는 시작 버튼 잠김), 파리 수 슬라이더와 예상 시간, 실제 연구(접힘). 모델 설명은 답을 알려 주므로 결과 화면에만 | 시작 요청을 브라우저에서 가로채 내용(세트·파리 수·가설)만 확인, 실험이 생기지 않으면 15초 뒤 안내 — 실제 실험은 돌리지 않음 |
| ③ 결과와 3D 다시 보기 | 진행 막대·남은 시간·멈추기, 내 가설과 판정(색), 쌍 비교(A가 더 큼·비슷함·A가 더 작음 쌍 수), 동전 던지기 확률, 쌍 점 그래프(선호 지수는 +1·-1이 어느 냄새·색인지 표시), 시행 버튼 → 실험실 안 영상에서 다시 보기와 재생 막대, 새 시행 자동 보기, 탐구 보고서 열기, 같은 질문으로 다시 실험, 전체 숫자 표·모델 설명·실제 연구(접힘) | 방울 크기 기록으로 다시 보기, 닫은 뒤 원래 영상·재생 막대 복귀 |

- 영상 스트림은 탭마다 하나만 씁니다(브라우저가 서버 하나에 연결 6개까지): 실험실이 열리면 원래 영상을 끊고
  결과 화면에서만 실험실 영상을 켭니다.
- 보고서의 "실험 전에 고른 예상"도 세트의 가설 문장을 씁니다(예상이 기본 방향과 다를 때 "B의 '처음 먹기(초)'가
  A보다 클 것이다" 대신 "냄새가 없는 B가 설탕을 더 빨리 먹기 시작할 것이다").
- 옛 칸에서 시작된 냄새 실험 두 번(18:25)은 예상이 "A가 더 크다"(A>B)로 저장되어 "가설과 반대로 나왔습니다"로
  판정됩니다. 실제 결과는 냄새가 있는 A가 더 빨랐습니다.

## 남은 것

- ~~새 자극: 온도 구역, 쓴맛 첨가물, 반복 그림자, 빛으로 켜는 도파민 구역~~ → F절에서 넣음
- GPU 실행기 — 파리 수백 마리가 필요해지면 냄새·먹이 세트부터(겹눈 없음)
- 쉬운 설명 모드, 사진 저장(D절에서 남은 것)

---

# F. 초파리를 실제에 가깝게 — 추천 10개 진행 (2026-09-17 밤)

사용자 요청: "개선할만한 점들 추천좀" → 추천 목록에 "2는 안해도될거같음 나머지 진행". 진행 중에 "영양 없는 단맛도 배를
채울까? — 영양이 있을 때만 배를 채우도록 짜 놓았으니 연구 질문이 아님, 없애도 될 듯"(세트 삭제). 외부 데이터(DoOR,
hemibrain, 46.5 MB)는 파일·출처·크기·라이선스를 적어 허락을 받은 뒤 받았습니다. 근거 문헌은 RESEARCH.md 15절.

| # | 추천 | 한 것 | 확인 |
|---|---|---|---|
| 1 | 서버를 다시 켜도 초파리 유지 | ✅ `flyplay/session.py`: 30초마다와 끝낼 때 저장, 시작할 때 복원(`--session`, `--fresh`). 파일 잠금, 지나온 길은 이어 쓰기, 배선 지문이 다르면 새 파리 | 점검 17개(스크립트) + 8011 포트 뷰어를 강제로 끈 뒤 다시 켜서 방·배고픔·시계·가치·길·결과표 유지, 두 번째 뷰어 거부 |
| 2 | 기록에 코드 버전 | 사용자 판단으로 하지 않음 | — |
| 3 | 실제 수용체 반응 | ✅ DoOR 2.0.1 → `flyplay/data/door_odorants.csv`. 식초 = 아세트산+에틸아세테이트+아세토인. 3-옥탄올의 Or13a 오염 반응 제거 | `13_mb_check.py` 9절 |
| 4 | 겹눈을 더 자주 | ✅ 움직이는 자극이 있을 때만 100 Hz(10번과 함께). 따로 올리면 이득 없이 배경 실험만 느려짐 | 10번 |
| 5 | 효과 크기와 필요한 파리 수 | ✅ 판정·보고서·실험실에 "차이의 크기"와 "확실히 보이려면 조건마다 약 N마리". 지난 실험의 권장 수를 파리 수 슬라이더 옆에 | 기존 실험 9개로 계산 확인 |
| 6 | 벽에 막히는 냄새 | ✅ `WalledOdorField`: 장애물을 돌아가는 최단 경로 길이의 역제곱. 보이는 곳은 예전과 비트 단위로 같음 | 기하 점검 10개, 막다른 길 미로 먹이 찾기 4/6(중앙값 40초) → 6/6(16.6초) |
| 7 | 실제 초파리 전략 | ✅ 벽 따라 걷기(Soibam 2012), 개체 차(좌우 편향 Beta(4.2,4.2), 속도·벽 선호), 주변 탐색 반경이 커짐, 주변 탐색이 중심복합체 추정값으로 돌아감 | 아래 측정 |
| 8 | 나침반과 장소 기억 | ✅ `flyplay/central_complex.py`: 16쐐기 나침반(확산 1.82e-3), 고리 뉴런 표지 학습, 경로 적분, 목표(시원한 곳). 낱눈 방위각은 렌더링으로 잼(`19_eye_azimuths.py`) | 아래 측정 — 장소 기억은 약함 |
| 9 | 커넥톰 버섯체 | ✅ hemibrain 케니언 세포 1733개(종류·엽·발톱 수 유지, 개체마다 재추첨), 엽마다 APL 되먹임 억제, 구획이 자기 엽만 읽음, 시각 케니언 세포 159개 | `13_mb_check.py` 38개 통과, 방 학습 확인 |
| 10 | 움직임 시각 | ✅ `flyplay/motion_vision.py`: T4/T5형 상관 감지기(Borst 2018 상수), 시운동 반응, 머리 위 그림자 감지(순수한 어두워짐). flyvis는 CPU에서 너무 느려 쓰지 않음 | 렌더링 보정 + 아래 측정 |
| 작은 개선 | APL, 주둥이 접촉, 새 자극, 개체 차 | ✅ 온도 구역(경계 되돌기, 열 처벌), 빛 도파민 구역, 쓴맛 첨가물, 순엽이 방울에 닿아야 마심 | 아래 측정 |
| 운영 | 시범 실행, 예상 시간, 성능 | 예상 시간은 최근 실험 세 개의 실제 속도 중앙값으로. 성능은 PERFORMANCE_PLAN.md 0절(적용 완료). 재설계 세트 시범 실행과 새 세트는 사용자가 시작 | — |

**모두 끄면 예전 파리.** `SandboxConfig(brain="random", individuality=False, wall_following=False,
search_radius_start=12, central_complex=False, labellum_contact=False)`는 바꾸기 전의 행동 지문과 비트 단위로 같습니다
(기능을 하나 넣을 때마다 확인).

**새 A/B 세트 5개**(모두 시범 실행 안 함): `optomotor`(줄무늬 따라 돌기), `shadow_speed`(그림자와 걷는 속도),
`place_memory`(표지로 시원한 칸 기억), `bitter_food`(쓴맛과 먹는 시간), `light_learning`(빛 도파민으로 냄새 학습).
방 프리셋 50 → 61개.

## 측정 (새 기능, 파리 4마리씩)

같은 시드의 켬/끔 쌍입니다(스크래치 `behaviour_calibrate.py`,
`brain_learning_check.py`, `walls_behaviour.py`, `tune_calibrate.py`).

| 기능 | 조건 | 켬 | 끔 | 실제 초파리 |
|---|---|---:|---:|---|
| 벽 따라 걷기 | 빈 방 120초, 바깥 1/3에 있던 비율 | 0.89 (0.80–0.98) | 0.72 (0.66–0.78) | 0.90–0.95 (Soibam 2012) |
| 그림자 | 10초마다 1초 간격 5번, 60초 평균 속도 | 16.1 mm/s, 얼어붙음 2–8초 | 14.1 mm/s | 빨라짐(Gibson 2015) |
| 쓴맛 | 쓴맛 0.6 섞은 식초 설탕, 먹은 시간 / 식초 가치 | 9.0초 / −0.22 | 12.8초 / +0.75 | 덜 먹고 짝지은 냄새를 피함(Das 2014) |
| 빛 도파민 | 옥탄올 + 처벌 빛 3분, 옥탄올 / MCH 가치 | −0.13~−0.23 / −0.02~−0.13 | — | 빛만으로 기억(Aso & Rubin 2016) |
| 커넥톰 뇌 | `sugar_odour` A 조건 시험 PI / MCH 가치 | +0.90 / +0.50 | 무작위 배선 +0.97 / +0.04 | 옥탄올·MCH는 수용체가 겹침 |
| 커넥톰 뇌 | `shock_odour` A 조건 시험 PI | +0.02 | 무작위 배선 +0.05 | 전기 세트는 예전 뇌로도 작음(E절 시범 +0.07) |
| 벽에 막히는 냄새 | 막다른 길 미로, 90초 안에 먹이 찾은 파리 | 6/6 (중앙값 16.6초) | 벽 통과 4/6 (40초) | — |
| 시운동 반응 | 반시계 60°/s 원통 30초, 반시계 방향 회전 속도 | 이득 1.0: +17.1~+21.0°/s (4마리 모두) | 멈춘 원통 −19.8~+18.8°/s | 따라 돎 |
| 장소 기억 | 뜨거운 바닥 45초 × 8번, 마지막 두 시행에 시원한 칸까지 걸린 시간 | 표지 있음 14.3초, 32번 중 9번 못 찾음 | 표지 없음 27.5초, 13번 | 점점 빨라짐(Ofstad 2011) |
| 주둥이 접촉 | 켠 상태의 식초 설탕 60초 (쓴맛 대조군) | 8.9–16.8초 먹음 | 재지 않음 | 순엽이 닿아야 마심 |

**정한 값.** 측정으로 고른 것: 시운동 이득 0.3 → **1.0**(0.3에서는 4마리 중 2마리가 원통과 반대로 벽을 계속 돎),
나침반이 표지를 따르는 세기 4.0 → **0.4**와 표지를 믿기 시작하는 뚜렷함 (0.1, 0.4) → **(0.3, 0.7)**(1.0에서는 마지막
두 시행 39.2초로 오히려 느려짐; 4.0은 이번 비교에 넣지 않음). 숫자와 이유는 코드 주석에 있습니다.

**장소 기억은 약합니다.** 표지가 있는 파리는 마지막 두 시행에서 4쌍 모두 빨랐지만 처음 두 시행부터 이미 빨랐습니다
(25.4초 대 34.1초). 시행이 거듭되며 줄어든 폭은 표지 있음 −11.1초, 없음 −6.6초입니다. 먼저 해 보고 버린 방식 셋은
`flyplay/central_complex.py` 모듈 설명에 있습니다.

**점검.** `13_mb_check.py` 38/38, 기능을 모두 끈 초파리의 행동 지문 3개 방 모두 예전과 같음(상수를 정한 뒤 다시 확인),
세션 저장·복원 17/17, `10_sense_check.py` 10/10.

## 남은 것

- **시범 실행(사용자가 시작):** 새 세트 5개, 재설계한 세트 7개(`drop_size`, `same_fly_faster`, `maze_practice`,
  `shock_colour`, `sugar_colour`, `stronger_shock`, `cue_needed`). 모든 세트가 이제 새 초파리(커넥톰 뇌, 벽 따라 걷기,
  중심복합체, 주둥이 접촉)로 돌므로 E절 시범 결과와 바로 비교할 수는 없습니다.
- ~~성능 개선: PERFORMANCE_PLAN.md에서 고르기~~ → X1·C1·B1·E1·P1 적용(2026-09-18). 방에 따라 파리당 −6~−26%, 움직이는
  자극이 있는 방은 겹눈만으로 −35~−41%, 결과는 비트까지 같음(PERFORMANCE_PLAN.md 0절). noslip 끄기(−27%)는 행동이
  달라지므로 켠 채로 두기로 했습니다.
- 모델: 장소 기억 강화, 주둥이 접촉 켬/끔 비교, 시운동 이득 1.0에서 그림자 다시 재기, 빛 학습 시험 PI 확인.
- ~~삭제한 세트의 기록 `out/experiments/20260917-221302-calories_fill/`~~ → 지웠습니다(2026-09-18, 33개 파일 6.4 MB).
