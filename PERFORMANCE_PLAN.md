# 성능 개선 계획 — 샌드박스 파리 (배경 A/B 실험과 라이브 뷰어)

2026-09-17 21:50~23:40에 측정·분석했습니다. **프로젝트 코드는 바꾸지 않았습니다.** 개선안은 모두 스크래치
디렉터리의 시제품 코드를 실행 중인 `Sandbox` 인스턴스에 덧씌워(monkeypatch) 잰 것입니다. 수치는 모두 이 노트북에서
직접 잰 값이고, 재지 않은 값은 **추정**이라고 쓰고 그것을 확인할 측정을 함께 적었습니다.

행동 영향 분류:

- **(a) 비트 동일** — 같은 부동소수점 연산을 같은 순서로 한다. 스텝별 비교와 지문으로 확인한다.
- **(b) 통계적으로 같음** — 비트가 달라지므로 행동 검증 계획이 필요하다.
- **(c) 모델이 바뀜**.

## 0. 진행 상황 — 2026-09-18 적용

**X1·C1·B1·E1·P1을 넣었습니다.** 다섯 모두 (a), 즉 같은 부동소수점 연산을 같은 순서로 합니다. 확인한 것:

- 걷기 제어기: 실제 주행에서 녹음한 호출 5000번(발 걸림 보정 10회 포함) — 관절각·접착·보정값·CPG 위상 전부 일치. 218 → 125 µs/호출.
- 겹눈: 실제로 그린 화면 120장 전부 일치, 생성할 때 FlyGym 경로와 자체 대조. 두 눈 한 번 5.81 → 1.75 ms.
- 방 10개 × 시드 2개 × 10초, 게이트·시각 캐시 켬/끔: `qpos`·판독값·시냅스·집계 20쌍 모두 일치.
- 벽 돌진 스트레스(반사 끔, 벽·블록·벽): 1400스텝 중 접촉 798·뒤집힘 338, `qpos` 매 스텝 일치. 평균 20개 중 19.2개가 게이트로 꺼져 있었습니다.
- 기능을 모두 끈 초파리의 행동 지문 3개 방 모두 이전과 같음. `13_mb_check.py` 38/38, `10_sense_check.py` 10/10, 세션 17/17.
- 워커 고정: 6워커가 서로 다른 코어 6개에 `NUMBA_NUM_THREADS=1`로 올라감(`pin_check.py`).

방마다 빨라진 정도(파리 한 마리, ms/시뮬레이션초, 게이트·캐시 끔 → 켬. 겹눈은 두 팔 모두 새 경로):

| 방 | 끔 (시드 0/1) | 켬 | 변화 |
|---|---:|---:|---:|
| `t_maze` | 1283 / 1235 | 949 / 931 | −26% / −25% |
| `scented_sugar` | 979 / 991 | 784 / 758 | −20% / −23% |
| `obstacle_field` | 1022 / 1031 | 811 / 823 | −21% / −20% |
| `corridor` | 1086 / 1079 | 953 / 924 | −12% / −14% |
| `dead_end_maze` | 1063 / 1055 | 916 / 956 | −14% / −9% |
| `heat_maze` | 983 / 924 | 857 / 796 | −13% / −14% |
| `drum_turning` | 1236 / 1219 | 1138 / 1150 | −8% / −6% |
| `shadow_sugar` | 1251 / 1212 | 1140 / 1083 | −9% / −11% |

겹눈만 따로(FlyGym 경로 → 모으기, 지문 동일): `scented_sugar` 811 → 775(10 Hz), `shadow_sugar` 1756 → 1133,
`drum_turning` 1998 → 1176(100 Hz).

### 적용 뒤 시간 분해 (`scented_sugar`, P 코어 3번, 10초)

한 걸음 전체 1206 → **829 ms/시뮬레이션초**.

| 항목 | 적용 전 | 지금 |
|---|---:|---:|
| MuJoCo 물리 `mj_step` | 815.6 | 617.4 (81%) |
| ㄴ 제약 풀이 | 225.9 | 213.1 |
| ㄴ 충돌 검출 | 313.5 | **113.7** (좁은 단계 266.2 → 64.8) |
| ㄴ noslip용 `efc_AR` 투영 | 75.0 | 88.9 |
| ㄴ 기구학 / 관성 / 제약 생성 / 적분 | 49.6 / 33.2 / 35.9 / 27.3 | 51.7 / 32.5 / 37.0 / 26.9 |
| 겹눈 | 98.0 | **30.7** (그중 GPU 렌더 22.8) |
| 걷기 제어기 | 79.1 | **48.5** |
| 버섯체 | — | 23.4 (시각 전단 13.5 → 4.0) |
| 경계구 게이트 자체 | — | 2.7 |

### 남은 것에 대한 판단 (2026-09-18 측정)

- **S1 noslip 끄기: 하지 않습니다 — 측정으로 기각(2026-09-18).** 속도는 확실히 큽니다(`scented_sugar` 829 → **607
  ms/시뮬레이션초, −27%**). 그러나 걷기 자체가 달라집니다. 아래 S1 절의 측정을 보세요: 직진 속도 −4.9%(합격선 3%),
  **최대 회전 −21.5%**(합격선 5%), 돌 때 제자리에서 도는 대신 2.15 → 7.19 mm 미끄러짐. 검증 1·2에서 이미 떨어지므로
  나머지를 돌릴 필요가 없었습니다.
- **센서 끄기: 해당 없음.** 이 모델은 MuJoCo 센서가 0개라 `mjDSBL_SENSOR`로 얻을 것이 없습니다(`t_maze` ±0%).
- **풀이 반복 상한: 해당 없음.** 접촉 중앙값 8개·제약 32행에 뉴턴 반복 중앙값 6회(상한 100)라 상한이 비용이 아닙니다.
- **P2(장애물 슬롯): 안 함.** 접촉 8개를 찾으려 쌍 756개를 훑는 값(약 30 ms/시뮬레이션초, 3~4%)이지만 "모든 시드가 같은 모델"이
  깨지고, 방이 슬롯보다 많은 항목을 쓰면 복원이 실패하는 새 실패 방식이 생깁니다.
- **E3(GPU 전원 설정): 거의 없음.** 지금 재 보니 눈 하나 렌더+읽기가 1.14 ms로 연달아 찍을 때와 비슷합니다(계획서가 걱정한 2.9 ms 아님).
- **X3(워커 시작): 2.6초/프로세스.** E1 뒤로 FlyGym의 어안·육각 JIT(2.26 + 0.35초)를 부르는 것은 `EyeReadout`의 자체 대조뿐입니다.
  워커에서 이 대조를 건너뛰면 그만큼 돌려받습니다(짧은 세트에서 약 2%).
- **X2(실험 중 뷰어 파리 멈추기): 14코어 중 1개.** 코드 없이 습관으로.
- **V1, S2**: 그대로.

---

## 1. 요약

### 배경 실험 풀: RTX로 렌더하는 스레드는 P 코어 4개에만 올라간다

가장 큰 병목은 파리 계산이 아니라 **코어 배치**입니다. RTX에 OpenGL 컨텍스트를 만들어 쓰는 스레드는 Windows가 P 코어
4개에만 두고, P 코어가 꽉 차도 E 코어로 넘기지 않습니다(같은 프로세스의 다른 스레드는 0.97코어로 E 코어도 씁니다).
워커는 물리·뇌·겹눈 렌더를 모두 주 스레드 하나에서 하므로 워커 14개가 **16코어 중 P 코어 4개**를 나눠 씁니다.

| 측정 (단일 프로세스, 10초, P 코어가 다른 풀로 꽉 찬 상태) | 그 프로세스가 쓴 코어 |
|---|---:|
| 파이썬 CPU 루프, GL 없음 | 0.96 |
| 같은 루프 + RTX OpenGL 컨텍스트 | **0.37** |
| 같은 루프 + Intel 내장 GPU 컨텍스트 | 0.95 |
| RTX 컨텍스트는 주 스레드에, 루프는 새 스레드에서 | 0.97 |
| RTX 컨텍스트 + E 코어 6번에 고정 / E 코어 8개에 고정 / LP-E 12번에 고정 | 0.94 / 0.98 / 0.95 |

같은 시각 다른 세션의 12워커 풀(`behaviour_calibrate.py`)은 워커당 0.17~0.36코어를 썼고, 10초 동안 코어 사용량이
**P 4.00 / E 0.89 (8개 중) / LP-E 0.59 (4개 중)**, GPU 사용률은 5%였습니다. 그 전 8워커 풀(`brain_learning_check.py`,
300 시뮬레이션초 × 16작업)은 파리당 0.353~0.391×, **합계 2.98×** 였습니다. RESEARCH.md §9.1의 "14프로세스 6.5×"는
21:48 이전 Intel 내장 GPU 렌더링 때 값입니다. RTX 전환 뒤 풀은 다시 재지 않았으므로, 그때 풀 처리량이 절반 가까이로
떨어졌을 가능성이 큽니다(**추정**, 4장 1단계에서 확인).

### 파리 한 마리: 시간이 어디로 가나

실험 워커가 도는 그대로(`Sandbox.step` 100 Hz, `scented_sugar`) P 코어 3번에 고정해 잰 시뮬레이션 1초당 벽시계
시간입니다(`sandbox_breakdown.py`, 시뮬레이션 13~23초, 22:50 이전 코드).

| 항목 | ms / 시뮬레이션초 | 비율 |
|---|---:|---:|
| **MuJoCo 물리 `mj_step`** | **815.6** | **77.5%** |
| ㄴ 충돌 검출 (그중 좁은 단계 266.2) | 313.5 | 29.8% |
| ㄴ 제약 풀이 (Newton 약 2회 + noslip 5회) | 225.9 | 21.5% |
| ㄴ noslip용 `efc_AR` 투영 | 75.0 | 7.1% |
| ㄴ 기구학 49.6 · 관성 33.2 · 제약 생성 35.9 · 적분 27.3 | 146.0 | 13.9% |
| **겹눈** (10 Hz, 두 눈) | **98.0** | **9.3%** |
| ㄴ 렌더 + `readPixels` + 뒤집기 / 어안 보정(numba) / 육각 풀링(numba) | 47.1 / 38.9 / 6.9 | |
| **걷기 제어기** (500 Hz, 관측 포함) | **79.1** | **7.5%** |
| 버섯체 23.6 · 행동 규칙 12.1 · 나머지 파이썬 24.1 | 59.8 | 5.7% |
| 합계 (래퍼 포함 / 래퍼 없이) | 1052.5 / 1062.5 | 0.94× |

**충돌 검출(전체의 30%)의 약 2/3가 방 벽 몫입니다**(아래 P1을 적용하면 313.5 → 102.5 ms). 방 벽 4개는 길이 104 mm
상자라 경계구 반지름(`geom_rbound`)이 52.1 mm이고, 방 안쪽 반폭이 50 mm라서 MuJoCo가 충돌 쌍마다 먼저 하는 경계구
거르기를 방 어디서나 통과합니다. 그래서 **다리 메시 36개 × 벽 4개 = 144쌍이 물리 스텝마다(초당 1만 번) 볼록체 거리
계산까지 가고**, 반사 덕분에 거의 언제나 "접촉 없음"으로 끝납니다. 미로의 긴 장애물 벽도 같습니다(`POOL_SIZES` 주석의 "주차된 geom은 경계로 걸러진다"는 맞지만 방 벽과 배치된
긴 벽은 걸러지지 않습니다). 걷는 상태 300개에서 `mj_collision`이 27.5 µs였고, 상자를 모두 5 m 밖으로 치우면 7.9 µs였습니다.

### 권장 상위 5개

| 순위 | 제안 | 분류 | 측정한 효과 |
|---|---|---|---|
| 1 | **X1** 워커를 코어마다 고정 | a | 고정한 RTX 프로세스가 0.37 → 0.94~0.98코어. 풀 합산 약 3× → 약 10× (**추정**) |
| 2 | **P1** 벽·장애물 경계구를 파리 근처에서만 켜기 | a | 파리당 −11~−29% (방 4종), 지문 동일 |
| 3 | **E1** 어안 보정과 육각 풀링을 한 번의 직렬 gather로 | a | 두 눈 읽기 7.07 → 1.96 ms (코어 고정 시), 파리당 약 −4%; 움직이는 자극이 있는 방에서는 훨씬 큼 |
| 4 | **C1 + B1** 제어기 스플라인 일괄 평가, 시각 전단 재사용 | a | 제어기 88.7 → 51.3 µs/호출, 시각 전단 13.5 → 1.9 ms/시뮬레이션초 |
| 5 | **S1** noslip 반복 끄기 | **b** | P1~B1 위에 추가 −17.5% — 행동 검증 뒤 결정 |

P1·E1·C1·B1을 함께 덧씌운 측정 (지문 전부 기준선과 동일, 22:50 이전 코드, 시뮬레이션 0~20초):

| 방 · 코어 | 지금 | P1+E1+C1+B1 | 변화 |
|---|---:|---:|---:|
| `scented_sugar` · P 3번 (두 번 잰 평균) | 1181 | 886 | −25% |
| `t_maze` · P 3번 (두 번 잰 평균) | 1495 | 946 | −37% |
| `scented_sugar` · E 6번 (15초) | 1436 | 1048 | −27% |
| `scented_sugar` · LP-E 13번 (15초) | 1539 | 1126 | −27% |

(ms / 시뮬레이션초.) 23:20 코드 스냅샷(연결체 뇌, 벽을 돌아가는 냄새, 움직이는 원통)에서도 `t_maze`,
`obstacle_field`, `drum_turning`의 지문과 벽 돌진 스트레스 시험이 기준선과 같았습니다(그 시각의 속도는 다른 풀 때문에
버렸습니다). 뷰어와 같은 루프(4스텝 + 1280×720 프레임 + JPEG)는 **0.77× → 1.02×** 였습니다.

## 2. 측정 방법

### 기계와 그때의 상태

- Intel Core Ultra 7 356H: **P 코어 4개(논리 0~3) + E 코어 8개(4~11) + 저전력 E 코어 4개(12~15), SMT 없음**(논리 16 =
  물리 16). P 코어는 L2 3 MB씩, E 코어는 4개 묶음마다 L2 4 MB, L3 18 MB는 0~11번이 공유하고 12~15번은 L3가 없습니다
  (`GetSystemCpuSetInformation`, `GetLogicalProcessorInformationEx`). Windows가 가장 선호하는 코어는 2번. 메모리
  32 GB LPDDR5X-8533, RTX 5060 Laptop 8 GB. AC 전원(충전 중), 전원 구성표 "SAMSUNG MODE"(이기종 스레드 예약 정책
  자동, 코어 파킹 없음 — `powercfg /qh`와 성능 카운터로 확인, 바꾸지 않음).
- MuJoCo 3.9.0, numba 0.67.0(OpenMP 계층, 16 스레드), NumPy 2.5.3, SciPy 1.18.1, Pillow 12.3.0, Python 3.12.
- 샌드박스 모델: nq 75, nv 74, 충돌 쌍 756(평면-메시 36, 상자-메시 720 = 벽 4 + 장애물 풀 16, 각 36쌍), 센서 0,
  energy 플래그 켜짐, Euler(74개 중 68개 자유도에 감쇠 → 암시적 감쇠), Newton, 희소 야코비안(auto), noslip 5. 다리 충돌
  메시는 꼭짓점 329~995개(볼록 헐 99~341개). 걷는 동안 접촉 6.3개, 제약 행 25.3개, 풀이 반복 7.2 = Newton 약 2.1 +
  noslip 5(허용치에 한 번도 닿지 않고 늘 상한까지).
- **뷰어(`09_web_viewer.py --sandbox --port 8010`)가 내내 라이브 파리를 돌리며 1.04~1.20코어를 썼습니다**(페이지가 없어도
  돕니다). 뷰어는 건드리지 않았습니다.
- `17_run_experiment.py`는 한 번도 돌지 않았습니다(시간 측정마다 직전에 `Win32_Process`로 확인). 그러나 **23:02부터 다른
  세션의 풀 두 개가 연달아 돌았고**, 그동안의 시간 측정은 모두 버렸습니다. 그동안은 비트 동일성 확인과, 코어 배치를
  보는 읽기 전용 관찰만 했습니다. 거꾸로 제 고정·HIGH 우선순위 측정이 23:02~23:29의 8워커 풀에서 P 코어 하나를
  가져갔으므로 그 풀의 2.98×는 조금 낮게 나온 값입니다.
- **측정 중에 프로젝트 코드가 바뀌었습니다**(22:56~23:19: 연결체 후각 전단, 벽을 돌아가는 냄새장, 원통·그림자, 세션 저장).
  한 번은 기준선과 개선안 실행 사이에 `sandbox.py`가 바뀌어 지문이 달랐습니다. 그 뒤 비교는 23:20에 `flyplay/`를 스크래치로
  복사한 스냅샷(`FLYPLAY_ROOT` 환경 변수)으로 했습니다. 표마다 어느 코드인지 적었습니다.

### 방법

- 시간 측정 프로세스는 P 코어 3번에 고정하고 HIGH 우선순위로 돌렸습니다. 처음 2번 코어에 보통 우선순위로 고정했을 때는
  같은 기준선이 783 → 1774 ms/시뮬레이션초로 흔들렸고(원인 미확인), 바꾼 뒤 기준선끼리 ±4% 안에 들어왔습니다. 옵션
  변형은 기준선과 번갈아 돌렸습니다.
- MuJoCo 단계 타이머(`mjData.timer`)는 `mjcb_time`에 numba `cfunc`로 만든 네이티브 시계(`QueryPerformanceCounter`)를
  달아 켰습니다. 파이썬 콜백이 아니라서 오버헤드가 물리 스텝의 1~3%입니다.
- 파이썬 쪽은 cProfile 대신 인스턴스 메서드에 `perf_counter` 래퍼를 씌워 쟀습니다(합계가 래퍼 없는 시간과 2% 안).
- **비트 동일성 지문**: 매 스텝 `qpos`, 새로 읽은 겹눈 판독값 전부, 끝의 시냅스 가중치, `trial_metrics()`를 SHA-1로 묶어
  기준선과 비교. 제어기는 실제 실행에서 기록한 관측 5000개를 두 제어기에 열린 루프로 넣고 출력·내부 상태를 매번
  `np.array_equal`로 비교.
- 스크립트는 모두 `C:\Users\pdaej\AppData\Local\Temp\claude\C--Users-pdaej-fly\6e0ddbf6-a55c-473a-b595-63d47cd19158\scratchpad\perf\`
  에 있습니다(세션 스크래치라 지워질 수 있으니 쓸 것은 옮겨 두세요). 원자료 요약은 같은 곳의 `notes_measurements.md`.

| 스크립트 | 무엇을 | 예 |
|---|---|---|
| `mjtimer.py` | MuJoCo 단계 타이머용 네이티브 시계 | `import mjtimer; mjtimer.install()` |
| `phys_bench.py`, `sweep.py` | 제어기+물리만(눈·뇌 없음), 방 중앙 15 mm 궤도 보행; 옵션 변형을 기준선과 번갈아 | `sweep.py 3 3 noslip0 dense` |
| `sandbox_breakdown.py` | `Sandbox.step` 항목별 시간 + 단계 타이머 | `sandbox_breakdown.py 3 t_maze 10 lean` |
| `collision_bench.py` | 기록한 상태에서 `mj_collision`만; 헐 크기 변형 | `collision_bench.py 3 32` |
| `gate_check.py`, `gate_stress.py` | P1 시제품 지문·속도; 반사를 끄고 벽·블록에 돌진 | `gate_stress.py gate 3` |
| `eyes_bench.py`, `gpu_gap_bench.py` | 겹눈 파이프라인 조각; 물리 뒤 첫 렌더 | `eyes_bench.py - 1` |
| `controller_bench.py` | C1 비트 동일성과 속도 | `controller_bench.py 3` |
| `lean.py`, `combined_check.py` | P1+E1+C1+B1 시제품과 지문 비교 | `combined_check.py lean t_maze 20 3` |
| `placement_probe.py`, `qos_probe.py`, `pool_util.py` | 코어 배치, EcoQoS 상태, 돌고 있는 풀의 코어 사용 (읽기 전용) | `placement_probe.py rtx 6` |
| `viewer_bench.py`, `encode_thread_bench.py`, `status_bench.py` | 뷰어 프레임 비용 | `viewer_bench.py -` |
| `probe.py`, `misc_bench.py`, `cpu_classes.py`, `model_info.py` | 3프로세스 동시, 시작·출력 비용, 코어 구조, 모델 구성 | `probe.py 3 0 1 3` |
| `r1.py` | 23:20 스냅샷에서 다시 잴 묶음 (아래 "남은 측정") | `r1.py` |

### 검증할 때 주의 — RTX 렌더링도 실행마다 드물게 다르다

벽 돌진 스트레스 시험을 **같은 코드로 두 번** 돌렸더니 `qpos`는 매 스텝 같았지만 겹눈 렌더 140번 중 1번에서 낱눈 하나의
값이 6.5e-5(8비트 한 단계를 픽셀 수로 나눈 크기) 달랐습니다. 20초짜리 일반 실행은 반복해도 같았고, 23:20 스냅샷에서 같은
시험의 기준선과 게이트 실행은 판독값까지 같았습니다. 그러니 **`qpos` 지문을 엄격한 기준**으로 삼고, 판독값만 다르면 기준선을
한 번 더 돌려 렌더러 잡음인지 가리세요. CLAUDE.md의 "색 절제 네 조건은 항등식이라 경로가 같아야 하고, 다르면 버그"라는
판정에도 드물게 이 잡음이 섞일 수 있습니다.

### 남은 측정

23:20 코드(연결체 뇌, 벽을 돌아가는 냄새장, 원통·그림자)의 항목별 시간은 23:02부터 23:43까지 다른 세션의 풀이 계속 돌아
깨끗하게 재지 못했습니다. 특히 원통·그림자 방(눈 100 Hz)에서의 E1 효과는 부분 측정값을 더한 **추정**입니다. 기계가 조용할 때
아래 명령이 23:20 스냅샷으로 기준선/개선안 짝 20개(샌드박스 항목별 시간, `drum_turning`·`t_maze`·`scented_sugar`, noslip,
장애물 슬롯, E 코어, 뷰어 루프)를 한 프로세스씩 돌려 `r1.log`에 남깁니다(약 15분). 실험이나 다른 풀이 보이면 시간을 재지 않고 멈춥니다.

```bash
cd C:\Users\pdaej\AppData\Local\Temp\claude\C--Users-pdaej-fly\6e0ddbf6-a55c-473a-b595-63d47cd19158\scratchpad\perf
C:\Users\pdaej\fly\.venv\Scripts\python.exe r1.py
```

## 3. 제안 목록

| ID | 제안 | 대상 | 기대 효과 | 분류 | 작업량 | 위험 | 우선순위 |
|---|---|---|---|---|---|---|---|
| X1 ✅ | 워커를 코어마다 고정 | 풀 | 풀 약 3× → 약 10× (추정) | a | 작음(~20줄) | 낮음 | 1 |
| P1 ✅ | 벽·장애물 경계구 근접 게이트 | 풀·뷰어 | 파리당 −11~−29% (측정) | a | 작음(~40줄) | 중: 순간이동 경로 누락 시 벽 통과 | 2 |
| E1 ✅ | 어안+육각 풀링 직렬 gather | 풀·뷰어 | 파리당 약 −4% (측정), 움직이는 방에서 약 −400 ms/시뮬레이션초 (추정) | a | 작음(~60줄) | 낮음 | 3 |
| C1 ✅ | 제어기 스플라인 일괄 평가 | 풀·뷰어 | 파리당 약 −2~3% (측정) | a | 작음(~50줄) | 낮음 | 4 |
| B1 ✅ | 시각 전단 결과 재사용 | 풀·뷰어 | 파리당 약 −1% (측정) | a | 아주 작음 | 낮음 | 5 |
| X2 | 실험 중 뷰어 일시정지, 워커 수 | 풀·뷰어 | 코어 1개 (측정: 뷰어 1.04~1.20코어) | a | 사용 습관 | 낮음 | 6 |
| V1 | 뷰어 프레임 가볍게 | 뷰어 | 프레임 7.0 → 4.5 ms 등 (측정) | 표시만 | 작음 | 낮음 | 7 |
| P2 | 프로토콜이 쓰는 장애물 슬롯만 컴파일 | 풀 | 물리 −4% (측정) | a | 중간 | 중: "모든 시드가 같은 모델" 가정 | 8 |
| X3 | 워커 시작 비용 | 풀 | 워커당 2.8 s, 작업당 약 1 s (측정) | a | 작음 | 낮음 | 9 |
| E3 | GPU 전원 관리 "최대 성능 선호" 시험 | 둘 다 | 눈 렌더 한 번 최대 약 2 ms (추정) | a | 설정 | 낮음 | 선택 |
| S1 | noslip 끄기 | 둘 다 | P1~B1 위에 추가 −17.5% (측정) | **b** | 검증이 큼 | 높음 | 1~5 뒤 결정 |
| S2 | 물리 dt 2e-4 | 둘 다 | −40~−47% (측정) | **c** | 매우 큼 | 매우 높음 | 보류 |

### X1. 워커를 코어마다 고정 — (a), 우선순위 1

**원인.** 1장의 측정 그대로입니다. RTX(NVIDIA) OpenGL 컨텍스트를 만들어 쓰는 스레드는 스케줄러가 P 코어에 두고, P 코어가
포화돼도 E·LP-E 코어로 옮기지 않습니다. 같은 프로세스의 다른 스레드(0.97코어)나 Intel 내장 GPU 컨텍스트(0.95코어)는 E 코어로
갑니다. **CPU 친화도를 지정하면 그대로 따릅니다**(E 6번 고정 0.94코어). 워커의 전원 제한(EcoQoS) 상태는 꺼져 있었고 우선순위는
보통이었습니다(`qos_probe.py`). 뷰어의 시뮬레이션 스레드도 렌더러를 만들어 쓰므로 P 코어에 묶이고, 풀이 도는 동안 0.30코어로
떨어졌습니다. 렌더링만 별도 스레드로 빼는 방법도 있지만 친화도 지정이 훨씬 간단합니다.

**방법.** `17_run_experiment.py`의 `ProcessPoolExecutor`에 `initializer`를 달아 워커마다 코어 하나를 줍니다.

```python
def _pin_worker(cores):            # cores: multiprocessing Queue, initargs로 전달
    psutil.Process().cpu_affinity([cores.get()])
# 14 워커: P 1~3, E 4~11, LP-E 12~14. P 0번(뷰어·데스크톱)과 LP-E 15번은 비워 둔다.
```

- 고정된 워커 안에서 FlyGym의 병렬 어안 보정(OpenMP 16 스레드)이 한 코어를 나눠 쓰므로(측정 1.90 ms, 1스레드 1.82 ms와 같음)
  E1 전까지는 워커에 `NUMBA_NUM_THREADS=1`을 주는 편이 같은 결과에 스레드 낭비가 없습니다(어안 보정은 픽셀 복사라 스레드 수와
  무관하게 결과가 같음).
- 대안인 "워커는 내장 GPU로 렌더"는 E 코어를 쓰게 하지만 눈 렌더가 10.9~16.5 ms로 느려지고 픽셀이 달라져 (b)입니다. 권하지 않습니다.

**기대 효과 (추정).** 단독으로 잰 샌드박스 파리 속도(P 0.85×, E 0.70×, LP-E 0.65×)를 코어에 그대로 더하면
3×0.85 + 8×0.70 + 3×0.65 ≈ **10×**, P1~B1까지 적용하면(P 1.13×, E 0.95×, LP-E 0.89× 측정) ≈ **13.7×**. 지금은 P 코어
4개 몫인 약 3~3.5×.
14코어가 동시에 돌 때의 전력·온도 제한은 재지 않았고, 3프로세스 동시 측정(P 0·1·3번, E 4·5·6번)에서는 단독과 차이가 없었습니다.
RTX를 14프로세스가 함께 쓸 때 눈 렌더가 느려질 수 있습니다(다른 풀이 도는 중 눈 하나 3.2~9.8 ms, 조용할 때 0.58~3.0 ms —
그때는 P 코어 포화도 겹쳐 있었음).

**검증.** 4장 1단계의 풀 측정. 비트 동일성: 같은 시드로 고정 전/후 실행한 `records/*.jsonl`의 `metrics`와 `path`가 같아야
합니다(렌더러 잡음 규칙은 위와 같음). 풀이 도는 동안 코어별 사용률에서 E·LP-E 코어가 워커 수만큼 차 있는지 확인합니다.

### P1. 벽·장애물 경계구를 파리 근처에서만 켜기 — (a), 우선순위 2

**방법.** 물리를 진행하기 직전마다 흉부와 각 고체(방 벽 4개 + 배치된 장애물)의 바닥면 사각형 사이 거리를 재서, `GATE_MM`
안이면 원래 경계구(방 벽은 컴파일 값을 보관, 장애물은 `Room._apply_params`가 쓰는 `norm(geom_size)`), 밖이면 `1e-6`을 씁니다.
**0은 안 됩니다** — MuJoCo에서 rbound 0은 평면처럼 "거르지 않음"입니다.

```python
# flyplay/room.py (스케치)
GATE_MM = 8.0      # 측정: 흉부에서 발끝까지 최대 3.0 mm, 10 ms 이동 < 0.3 mm
FAR_RBOUND = 1e-6

def gate_bounds(self, x: float, y: float) -> None:
    m = self.sim.mj_model
    for gid, rect, live in self._solid_bounds():
        gap = math.hypot(max(abs(x - rect.cx) - rect.hx, 0.0), max(abs(y - rect.cy) - rect.hy, 0.0))
        m.geom_rbound[gid] = live if gap < GATE_MM else FAR_RBOUND

def ungate(self) -> None:          # 몸을 새 자리에 내려놓기 전에 전부 원래 값으로
    ...
```

- `Sandbox.step`: `walker.advance` 직전에 `room.gate_bounds(흉부 x, y)`.
- `Sandbox._settle_body`(따라서 `reset_fly`, `return_home`, `move_fly`, `restore`의 `_put_down`): 정착 물리 전에 `room.ungate()`.
  이 경로를 빠뜨리면 벽 옆에 내려놓은 파리가 조용히 벽을 통과합니다 — CLAUDE.md의 "stale bound가 접촉을 조용히 없앤다"와 같은
  종류의 무증상 오류입니다.
- 음수 `pair_margin`으로 쌍을 끄는 방법은 **오히려 느렸습니다**(스텝 73 → 86 µs): 좁은 단계가 그대로 돌고 더 오래 걸립니다.

**왜 비트 동일인가.** 경계구에서 걸러지는 쌍은 거리 계산을 해도 접촉을 만들 수 없는 거리에 있으므로 `d.contact`가 같고, 이후
제약·적분이 모두 같습니다. 렌더링은 `geom_rbound`를 쓰지 않았습니다(판독값 지문도 같음).

**측정.** 게이트만, P 코어 3번, 20초, 지문 해시 오버헤드 포함(22:50 이전 코드):

| 방 | 지금 | 게이트 | `mj_step` | 충돌 단계 | 지문 |
|---|---:|---:|---:|---:|---|
| `t_maze` | 1469 | 1039 (−29%) | 1077 → 657 | 566 → 146 | 같음 |
| `scented_sugar` | 1206 | 982 (−19%) | 826 → 611 | 315 → 101 | 같음 |
| `obstacle_field` | 1257 | 1059 (−16%) | 852 → 646 | 339 → 114 | 같음 |
| `corridor` | 1328 | 1186 (−11%) | 929 → 691 | 421 → 159 | 같음 |

`corridor`와 `t_maze`에서 흉부가 고체 바닥면에 0.8 mm, 1.2 mm까지 다가갔고 그때는 게이트가 켜져 있었습니다. 스트레스 시험
(`gate_stress.py`: 반사를 끄고 벽·블록·벽을 향해 `move_fly` 세 번): 이전 코드에서 접촉 389스텝·뒤집힌 채 378스텝, 23:20 코드에서
접촉 690스텝·뒤집힌 채 329스텝 — 둘 다 `qpos` 지문이 단계마다 같았습니다. 물리 벤치에서 벽 경계구만 줄이면 스텝 73 → 60 µs,
좁은 단계 17.4 → 4.65 µs였습니다.

**검증.** (1) 모든 프리셋 × 시드 2개 × 20초의 `qpos` 지문 게이트 켬/끔 비교, (2) 벽 돌진 스트레스 시험, (3) 디버그 플래그: N 스텝마다
게이트를 모두 푼 `MjData` 복사본에서 `mj_collision`을 돌려 접촉 목록이 같은지 assert. **위험**은 새로 생기는 순간이동 경로와,
앞으로 추가될 충돌 고체가 게이트 목록에서 빠지는 것입니다.

### E1. 어안 보정과 육각 풀링을 한 번의 직렬 gather로 — (a), 우선순위 3

**원인.** FlyGym의 `Retina._correct_fisheye`는 매 호출 512×450 픽셀마다 같은 좌표 변환을 다시 계산하고(`parallel=True`), 그
결과를 `_raw_image_to_hex_pxls`가 다시 훑습니다. 변환은 상수라서, 원본 픽셀 인덱스를 한 번 만들어 두면 매 호출에 필요한 것은
"낱눈에 속하는 픽셀 17만 개의 G 또는 B 값 누적"뿐입니다.

**측정** (`eyes_bench.py`, 22:50 이전 코드; 눈 파이프라인은 그 뒤 바뀌지 않음):

| 눈 하나 | 한가한 기계, 16스레드 | 1스레드 | P 코어 하나에 고정 |
|---|---:|---:|---:|
| `correct_fisheye` | 0.42 ms | 1.82 ms | 1.90 ms |
| `raw_image_to_hex_pxls` | 0.35 ms | 0.35 ms | 0.34 ms |
| 결합 gather (numba 직렬) | **0.31 ms** | 0.32 ms | 0.31 ms |
| 두 눈 한 번(렌더 포함) 지금 → 결합 | 4.03 → 2.38 | 7.55 → 2.40 | 7.07 → 1.96 |

결과는 100번 모두 `np.array_equal`로 같았고 샌드박스 지문도 같았습니다. 10 Hz 방(코어 고정)에서는 겹눈 전체가 98.0 → 44.6
ms/시뮬레이션초였고, 남은 것은 거의 렌더 대기(37.0)입니다. **23:00 이후 코드는 원통·그림자가 있으면 눈을 100 Hz로 읽습니다.**
코어에 고정된 워커라면 그때 두 눈 한 번은 렌더(물리 10 ms 뒤 눈 하나 0.95~1.50 ms, 측정) + 어안 1.90 + 육각 0.34 ms로 약
6.4~7.5 ms, 결합 gather로 약 2.5~3.6 ms라서 **시뮬레이션 1초에 약 400 ms를 줄일 것으로 추정**합니다(부분 측정값의 합;
`r1.py`의 `drum_turning` 짝으로 확인). 고정하지 않은 한가한 기계에서는 어안 보정이 0.42 ms라 이득이 약 90 ms로 줄어듭니다.
`drum_turning`에서도 지문은 같았습니다.

**방법.** `flyplay`에 `@numba.njit(cache=True)` 함수 둘: FlyGym과 같은 식으로 원본 인덱스표를 만드는 함수, 그리고 목적 픽셀을
**오름차순으로** 돌며 `vals[h, ch] += raw[src, ch + 1] / npix[h]`를 하고 끝에 `/ 255`하는 함수(누적 순서가 FlyGym과 같아야 비트가
같습니다; 범위 밖 픽셀은 0을 더하는 셈이라 건너뛰어도 같습니다). `Sandbox.step`, 색 조건화의 `_colour_trial_steps`,
`ForageEnv`가 `sim.get_ommatidia_readouts` 대신 씁니다. 생성할 때 무작위 이미지 하나로 FlyGym 경로와 같은지 자체 검사를 넣어
FlyGym 업그레이드에 대비합니다.

**검증.** 실제 렌더 1000장과 무작위 이미지에서 `array_equal`; 샌드박스 지문; `10_sense_check.py` 10/10.

### C1. 걷기 제어기 — 다리 6개 스플라인을 PPoly 한 번으로 — (a), 우선순위 4

**측정** (`controller_bench.py`, 실제 실행에서 기록한 관측 5000개): `FastTurningController.step` 88.7 µs → **51.3 µs**, 출력(관절각·
접착)과 내부 상태(보정값·CPG 위상)가 5000번 모두 같았습니다(발 걸림 보정 13회 포함). 조각: 다리 하나 스플라인 5.35 µs × 6, 계수를
쌓은 PPoly 한 번 5.75 µs, 스칼라 `np.interp` 0.71 µs, 스칼라 `np.clip` 1.74 µs, CPG 적분 6.6 µs, `_observe` 10.1 µs. 샌드박스에서
제어기 전체가 79.1 → 54.2 ms/시뮬레이션초였습니다.

**방법.** `__post_init__`에서 다리별 `CubicSpline` 계수를 마지막 축에 쌓아 `PPoly(c, x, extrapolate="periodic")` 하나를 만들고,
`step`에서 6개 위상을 한 번에 평가해 `[i, :, i]`를 고릅니다(원소마다 같은 합산이라 비트 동일). `neutral + magnitude * (psi -
neutral)`은 브로드캐스트 한 번. 다리별 `np.interp`는 NumPy 구현과 같은 식·경계 처리(`x <= xp[0]`, `x >= xp[-1]`, `x == xp[j]`)의
파이썬 float 연산으로 바꿉니다. `np.clip`과 CPG는 그대로 둡니다 — CPG의 `np.sin`·`sum`을 numba로 옮기면 SIMD 경로가 달라질 수
있어 (b)가 되고, 남는 이득은 1% 안팎입니다.

**검증.** 기존 비교 하네스(FlyGym 원래 경로와 6000번) + 행동 지문.

### B1. 시각 전단 재사용 — (a), 우선순위 5

`Sandbox.step`은 10 Hz 방에서 겹눈을 10스텝마다 읽고 같은 판독값 객체를 다음 9스텝의 `mb.step`에 넘기므로, `VisualFrontEnd`가
같은 입력으로 초당 100번(+ 텔레메트리의 색 가치 20번) 돕니다. 판독값 객체와 `silenced`가 같으면 이전 `VisualState`를 돌려주면
됩니다. 측정: 13.5 → 1.9 ms/시뮬레이션초, 지문 같음. 색 가치용 합성 판독값의 코드는 배선이 고정이라 한 번만 계산하면 됩니다.
(움직이는 방에서는 판독값이 매 스텝 새로 오므로 이득이 없습니다.)

### X2. 실험 중 뷰어 일시정지와 워커 수 — (a), 우선순위 6

- 뷰어는 페이지가 없어도 라이브 파리를 돌려 1.04~1.20코어를 계속 쓰고, RTX 컨텍스트 때문에 P 코어에 묶입니다. 배경 실험 중에는
  뷰어의 파리를 일시정지하세요 — X1 없이는 P 코어 몫(풀의 거의 전부)을 두고 다투고, X1 뒤에도 코어 하나를 돌려줍니다.
- 워커 수는 X1과 함께 14(P 0번과 LP-E 15번을 남김). 16코어가 모두 물리 코어라 SMT 고려는 필요 없습니다. X1 뒤 12(E·LP-E만)와 14를
  비교해 뷰어 반응성과 처리량 중 고르면 됩니다.
- 코어 종류별 속도(단독 측정): 물리+제어기만 P 800~880, E 1113~1129, LP-E 1218 ms/시뮬레이션초(E/P 0.75, LP-E/P 0.69). 샌드박스 파리
  전체는 GPU 대기가 코어와 무관해 격차가 줄어 P 1181(20초), E 1436, LP-E 1539(15초)입니다.

### V1. 뷰어 프레임 — 표시만 바뀜, 우선순위 7

측정 (`viewer_bench.py`, 고정 안 함, 배속 1·25 fps처럼 4스텝마다 1프레임):

| 1280×720 한 프레임 | ms | 960×540 |
|---|---:|---:|
| 렌더 + `readPixels` + 뒤집기 | 4.0~4.5 | 2.9 |
| `Image.fromarray` | 1.4 | 0.8 |
| JPEG q80 (Pillow = libjpeg-turbo) | 1.2 | 0.8 |
| 장면 색칠 / `update_scene` | 0.17 / 0.03 | 같음 |

뷰어와 같은 루프(4스텝 + 1280×720 프레임 + JPEG, `encode_thread_bench.py`, 23:00 무렵 코드): **0.77× → P1~B1 적용 시 1.02×**.
실시간은 P1~B1로 닿고, 여유가 더 필요하면:

- 뒤처질 때(`pacer.lag_s`가 커질 때) 25 → 15 fps로 자동으로 낮추기: 초당 약 70 ms (측정한 프레임 비용 × 10).
- 960×540 선택지: 프레임당 약 2.6 ms. 페이지가 `object-fit: cover`로 늘리므로 선명도만 손해.
- 신경 대시보드 payload(프레임마다 0.46 ms + JSON 0.34 ms)를 폴링 주기(250 ms)마다 한 번만: 초당 약 10 ms.
- 전기 구역 점선(`_draw_shock_outlines`)이 판 7개 방(`shock_band`)에서 프레임당 0.71 ms: 항목이 바뀔 때만 계산.
- X1과 X2의 코어 배치가 뷰어에도 가장 큰 영향입니다(풀이 도는 동안 뷰어 0.30코어 관찰).

### P2. 프로토콜이 쓰는 장애물 슬롯만 컴파일 — (a), 우선순위 8

물리 벤치에서 장애물 풀 16개를 빼면(쌍 756 → 180) 스텝 73.3 → 70.3 µs(−4%), `qpos` 지문 같음. P1 뒤에도 남는 쌍 순회 비용(스텝당
약 3.9 µs)의 대부분입니다. 워커는 프로토콜의 방 목록을 미리 알므로 필요한 만큼만 만들 수 있습니다. 다만 CLAUDE.md가 "모든 샌드박스
시드는 같은 모델(nq 75, 같은 geom·joint 이름)"을 재생의 전제로 적고 있어, geom 수가 달라지는 것이 재생·세션 복원·`solid_ids`에
영향이 없는지 먼저 확인해야 합니다.

### X3. 워커 시작 비용 — (a), 우선순위 9

측정 (`misc_bench.py`): 워커 프로세스마다 첫 `correct_fisheye` JIT 2.49 s + 육각 풀링 0.35 s, 작업마다 `Sandbox()` 0.55~0.60 s + 첫
겹눈 GL 컨텍스트 0.30~0.43 s + 정착 0.20 s. 60초 시행 하나짜리 작업(벽시계 70~100 s)에서 작업당 약 1%입니다. E1을 `cache=True`로
만들면 JIT가 디스크 캐시 읽기로 바뀝니다.

### E3. GPU 전원 상태 — 사용자 설정 시험 (선택)

측정 (`gpu_gap_bench.py`, 눈 하나): 연달아 0.58~0.86 ms, 물리 10 ms 뒤 0.95~1.50, 50 ms 뒤 1.72~2.54, **100 ms 뒤(10 Hz 눈)
2.87~3.04 ms** — 늘어난 몫은 거의 `glFinish` 대기(0.13 → 1.6~1.85 ms)로, GPU가 쉬는 사이 절전 상태에 들어갔다 깨는 시간으로
보입니다. NVIDIA 제어판의 프로그램 설정에서 `python.exe`의 "전원 관리 모드: 최대 성능 선호"를 시험해 볼 만하지만 시스템 설정이라
직접 하셔야 하고 효과는 **미측정**입니다.

### S1. noslip 끄기 — (b), P1~B1 뒤에 결정

**측정.** 물리 벤치: 스텝 73.6 → 63.8 µs(−13%). noslip은 허용치에 한 번도 닿지 않고 매 스텝 5회를 다 돌고, noslip이 필요로 하는
`efc_AR` 계산(7 µs)도 사라집니다. P1~B1을 적용한 샌드박스(같은 구간): 737.6 → **608.7 ms/시뮬레이션초(−17.5%)**, 제약 풀이 226 → 196,
투영 75 → 0.5. 부수 효과로 MuJoCo-Warp(noslip 없음)와 물리가 같아집니다.

**행동에 미치는 영향 — 측정(2026-09-18, 지금 코드).** noslip은 각뿔 마찰 근사가 남기는 접선 방향 미끄러짐을 없애는 뒤처리
패스입니다. 끄면 디딘 발이 미끄러져 다리가 미는 힘이 덜 전달됩니다.

*걸음 자체* (하행 신호를 직접 주고 행동 규칙을 뺀 상태, `noslip_straight.py`·`noslip_effect.py`):

| 시험 | 켬 | 끔 | 변화 |
|---|---:|---:|---:|
| 직진 [1.0, 1.0] 속도, 3.5초 × 6시드 (벽 앞 −45 mm에서 동쪽으로) | 18.36 mm/s | 17.46 | **−4.9%** (합격선 3%) |
| 최대 회전 [0.4, 1.6], 5초 × 4시드 | 223.3 °/s | 175.4 | **−21.5%** (합격선 5%) |
| 돌면서 밀려난 거리 (제자리 회전이면 0에 가까움) | 2.15 mm | 7.19 | **+234%** |
| 도는 동안 디딘 발이 미끄러진 거리 | 6.53 mm/디딘초 | 7.50 | +14.8% |
| 멈춤 [0, 0], 5초 | 0.65 mm | 0.56 | 둘 다 사실상 정지 |

*방 안의 파리 전체* (20초 × 4시드, 같은 시드끼리):

| 방 | 걸은 거리 | 먹은 시간 | 벽시계 |
|---|---:|---:|---:|
| `scented_sugar` | 154 → 146 mm (−5.5%) | 7.4 → 7.4 s | −21% |
| `t_maze` | 231 → 209 mm (−9.9%) | 2.1 → 2.7 s (+31%) | −9% |
| `corridor` | 239 → 197 mm (−17.5%) | 1.7 → 3.5 s (**+109%**) | −10% |

검증 1(직진 3% 이내)과 2(회전 5% 이내)에서 떨어집니다. 회전은 이 초파리의 주된 출력이라 — 조향이 곧 좌우 진폭 차이입니다 —
21% 손실은 학습·탐색 결과를 전부 옮깁니다. 나머지 검증은 돌리지 않았습니다.

**검증 계획** (noslip 5 vs 0, 같은 시드):

1. 직진 속도(하행 신호 [1, 1], 10초, 시드 5개): 기준 13.84 mm/s와 3% 이내.
2. 최대 회전 [0.4, 1.6]: 228°/s, 반경 4.7 mm(room.py)와 5% 이내.
3. 디딘 발끝의 미끄러짐: 스탠스 동안 `tarsus5` 수평 이동량 분포(새 지표) — 중앙값이 두 배를 넘지 않을 것.
4. 먹는 동안 정지 속도 중앙값 0.34 mm/s → 0.5 mm/s 이하.
5. 벽 반사: 12회 접근에서 접촉 0, 뒤집힘 0 (room.py의 측정 재현).
6. 실험 세트 두세 개(`smell_guides`, 냄새·전기 학습 세트 하나, 색 세트 하나)를 조건마다 16마리로 noslip 켬/끔 실행(사용자 실행):
   결정 지표의 쌍 차이 90% 신뢰구간이 그 지표의 `tolerance` 안에 들면 동등.

### S2. 물리 dt 2e-4 — (c), 보류

측정: 물리 벤치 벽시계 −40%(스텝당 비용은 80 µs로 비슷하고 스텝 수가 절반), P1~B1 적용 샌드박스 −47%(`scented_sugar` 886 → 473,
`t_maze` 946 → 507). 그러나 20초 이동 거리가 152~155 mm vs 178~188 mm, 먹은 시간이 11 vs 8~9 s로 행동이 뚜렷이 달랐습니다. FlyGym의
접착·관절 강성, 주둥이 스프링(90%까지 0.23 s), CLAUDE.md의 벽 뒤집힘·회전 반경이 모두 1e-4에서 잰 값이라 전부 다시 재야 합니다.

## 4. 단계별 실행 계획

시간 기준은 P 코어 3번 고정·HIGH 우선순위, 뷰어 일시정지, 다른 풀·실험이 없을 때입니다.

### 0단계 — 기준선과 점검 도구 (반나절)

- `scripts/19_speed_check.py`(가칭): 프리셋(`empty`, `scented_sugar`, `t_maze`, `corridor`, `obstacle_field`, `drum_turning`,
  `shadow_sugar`, `blue_shock`) × 시드 2개 × 20초의 (1) 매 스텝 `qpos` 지문, (2) 판독값 지문, (3) 시냅스 + `trial_metrics()` 지문,
  (4) ms/시뮬레이션초와 MuJoCo 단계 타이머(`mjtimer.py`를 `flyplay`로 옮김)를 `out/perf/<날짜>.json`에. `--compare <파일>`로 지문 차이 보고.
  벽 돌진 스트레스 시험도 한 항목으로.
- **합격**: 같은 코드로 두 번 돌려 `qpos` 지문이 전부 같다. 판독값만 다르면 세 번째 실행으로 렌더러 잡음을 가리는 규칙을 도움말에 적는다.

### 1단계 — 워커 코어 고정 X1 (반나절 + 사용자 실행 30분)

- `17_run_experiment.py`에 코어 고정 initializer와 워커의 `NUMBA_NUM_THREADS=1`. 고정을 끄는 `--no-pin`을 남겨 비교에 씁니다.
- 사용자가 같은 세트를 고정 끔/켬으로 (작업 14개, 20초씩, 재생 파일 없음):

```bash
C:\Users\pdaej\fly\.venv\Scripts\python.exe scripts\17_run_experiment.py --set smell_guides --flies 7 --seconds 20 --replay-flies 0 --no-pin
C:\Users\pdaej\fly\.venv\Scripts\python.exe scripts\17_run_experiment.py --set smell_guides --flies 7 --seconds 20 --replay-flies 0
```

  합산 속도 = 280 시뮬레이션초 ÷ `status.json`의 `elapsed_s`. 도는 동안 `qos_probe.py`로 코어 종류별 사용량을 봅니다.
- **합격**: 고정 켬이 끔의 2배 이상, E·LP-E 코어가 워커 수만큼 참, 두 실행의 `records`의 지표·경로가 같음. `POOL_SPEED_PER_FLY` 갱신.
  CLAUDE.md에 불변식 한 줄: "RTX OpenGL 컨텍스트를 쓰는 스레드는 친화도를 주지 않으면 P 코어에만 머문다(측정 0.37코어)".

### 2단계 — 비트 동일 묶음 C1 → B1 → E1 → P1 (하루)

- 비교 도구가 이미 있고 위험이 낮은 것부터.
- **합격**: 0단계 기준선과 모든 지문이 같음(프리셋 × 시드 전부); 벽 돌진 시험의 게이트 켬/끔 `qpos` 지문이 같고, 게이트 디버그 검사가
  1만 스텝 통과; `10_sense_check.py` 10/10, `13_mb_check.py` 전부 통과; `scented_sugar` 20초가 기준선보다 20% 이상, `t_maze`가 30% 이상
  빠름(22:50 코드에서 −25%, −37% 측정).
- CLAUDE.md "Invariants"에: rbound 게이트(0이 아니라 1e-6, 몸을 내려놓기 전 해제), E1의 누적 순서.

### 3단계 — 풀 재측정과 뷰어 (반나절 + 사용자 실행)

- 1단계 명령에 `maze_smell`과 `optomotor` 세트를 더해 다시 재고, `--jobs 12`와 `14`를 비교.
- V1 항목. **합격**: 풀 합산이 1단계 뒤보다 25% 이상 높음; 페이지를 연 채 `scented_sugar`·`t_maze`에서 페이지의 `speed_factor` ≥ 1.0.

### 4단계 — noslip 검증 연구 S1 (1~2일 + 사용자 실행)

- S1의 검증 1~6. **합격**: 모두 통과하면 기본값 변경을 제안하고, 바꾼 날짜를 RESEARCH.md에 경계로 적습니다(RTX 전환 때처럼 행동 지문이
  달라지므로). 하나라도 실패하면 보류하고 결과만 기록합니다.

### 보류

S2(dt 2e-4), P2(장애물 슬롯), X3(시작 비용), E3(GPU 전원 설정): 3단계 결과를 보고 필요할 때.

## 5. 하지 않을 것과 이유

| 하지 않을 것 | 이유 (측정) |
|---|---|
| MuJoCo-Warp로 옮기기 | GPU는 물리만 1024 월드 12.5×(RESEARCH.md §9.1). X1+P1~B1 뒤 CPU 풀이 약 13×(추정). 뇌·눈·제어기를 일괄 연산으로 다시 쓰고 noslip 없는 걷기를 재검증해도 비슷한 수준 |
| 워커를 내장 GPU로 렌더 | E 코어는 쓰게 되지만 눈 렌더 10.9~16.5 ms, 픽셀이 달라져 (b). X1이 같은 효과를 비트 동일하게 냄 |
| 솔버 허용오차·반복 줄이기 | tolerance 1e-6·1e-5: 반복 수 7.2 그대로, 스텝 74.0·73.6 µs(기준 73 안팎), 궤적만 달라짐. iterations 5: 74.9 µs. Newton은 이미 약 2회에 수렴 |
| CG·PGS 솔버, 조밀 야코비안 | 스텝 262 µs(반복 49) / 98.7 µs(반복 105) / 83.0 µs |
| island 끄기 | 71.5 µs(−2%)인데 비트가 달라짐 |
| multiccd·네이티브 CCD·midphase·energy 끄기, implicitfast | 열린 방에서 궤적이 같고 속도 차이 없음(72.4~76.5 µs). implicitfast는 이 모델에서 Euler와 비트까지 같았음 |
| Euler 암시적 감쇠 끄기 | 불안정: 접촉 88개, 11 s/시뮬레이션초 |
| 다리 충돌 메시 단순화(maxhullvert) | `mj_collision` 27.5 → 18.0(헐 32개)·14.1 µs(12개)지만 대부분 벽 쌍의 몫. 상자를 치우면 7.9 → 7.4·7.2 µs라 P1 뒤 이득 1 µs 미만에 모델 변경(c) |
| JPEG 인코딩을 다른 스레드로 | 뷰어 루프 1292 → 1257 ms(지금), 982 → 979 ms(P1~B1 뒤). JPEG가 1.2 ms뿐이고, 인코딩 스레드가 돌면 파이썬 루프가 0.47배로 느려질 만큼 GIL을 다툼 |
| simplejpeg·turbojpeg 도입 | Pillow가 이미 libjpeg-turbo로 1280×720 q80을 1.0~1.2 ms에 인코딩. 새 의존성에 비해 프레임당 1 ms 미만(**추정**) |
| Python 3.13 / free-threading | P1~B1 뒤 파리당 파이썬 몫 약 15%. 프로세스 풀은 3개 동시 측정에서 간섭이 없었고, numba·mujoco·scipy의 free-threaded 빌드 지원은 이 환경에서 확인하지 않음 |
| `mujoco.rollout`으로 한 프로세스에 여러 파리 | 물리는 C 스레드로 병렬이지만 500 Hz 제어기와 뇌가 GIL 아래 직렬이라 프로세스 풀보다 나을 수 없음(**추정**) |
| 버섯체 가중치를 행렬 하나로 | BLAS 합산 순서가 달라져 (b), 이득은 버섯체 전체 23.6 ms/시뮬레이션초 중 일부 |
| 재생 파일 압축·기록 형식 바꾸기 | 60초 재생 압축 38 ms(0.85 MB; 무압축 0.93 MB), JSONL 한 줄 0.28 ms, `qpos` 복사 0.4 µs/프레임 — 시행 벽시계의 0.1% 미만 |
| 작업 사이 컴파일된 모델 재사용 | 작업당 약 1 s(1%) 절약에 비해 파리 사이 상태 누수 위험 |
