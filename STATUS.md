# 진행 상황 — 2026-09-17

진행 중인 작업 두 갈래의 현재 상태와 재개 방법입니다. 프로젝트 전체 설명은
[README.md](README.md), 연구 주제 목록은 [RESEARCH.md](RESEARCH.md)에 있습니다.

| 갈래 | 상태 |
|---|---|
| **A. 버섯체 연합학습** (RESEARCH.md 9절 10번) | **완료.** 소거까지 넣어 역전 기준도 통과. 기억 라이브러리 있음 |
| **B. 다중감각 먹이 탐색** 커리큘럼 | 1·2단계 완료, 3단계 미시작 |

**다음 순서 (사용자와 합의):** ① 버섯체에 시각 연결 → ② 수정한 밀폐된 방 실험
(냄새 나는 설탕물 + 장애물). 문헌 검토([RESEARCH.md](RESEARCH.md) 13절)의 추천 순서를
따르기로 했고, **실행 계획은 [PLAN.md](PLAN.md)에 있습니다(승인됨, A-1 진행 중 — 단계별 진척은
PLAN.md의 ✅ 표시).**
요지: ①은 색·밝기의 의미 학습(Vogt 2014·2016 재현)이고, ②는 ①과 무관하게 벽 반사·
이류–확산 냄새장·실제 초파리 전략·시각 나침반과 목표 기억이 필요합니다. 계획 작성 중 측정한
결정적인 값 — 벽에 부딪힌 초파리는 0.35–2.3초 안에 뒤집힙니다(PLAN.md "이미 확인한 것").

**지금 떠 있는 뷰어:** http://localhost:8010 — **샌드박스**(PLAN.md C·D·E절). 사각 밀폐 방에 설탕·전기·
냄새·색 바닥·장애물(블록·긴 벽)을 지도에서 클릭으로 놓고 초파리 반응을 봅니다. 방 프리셋 50개(7묶음),
지도에 **지나온 길(10초~1시간·무한)과 히트맵**.

영상 아래 탭 세 개:
- **A/B 실험실** — 누르면 창 전체에 실험실이 열리고 한 화면에 한 단계씩 진행합니다.
  ① 질문 고르기(세트 22개: 첫 방 그림, A·B 한 줄, 재는 것, 예상 시간) → ② 가설 세우고 시작(A·B의 방 순서
  그림과 바뀐 값, 변인, 세트마다 문장으로 쓴 가설 3개 중 고르기, 파리 수와 예상 시간) → ③ 결과와 3D 다시
  보기(진행 막대·남은 시간·멈추기, 가설 판정과 쌍 점 그래프, 시행 버튼을 누르면 실험실 안 영상에서 **다시
  보기**, 새 시행 자동 보기, 탐구 보고서 열기). 실험은 `scripts/17_run_experiment.py`가 CPU 코어 14개로 배경에서
  돌리고 실험실을 닫아도 계속 돕니다. 탭에는 최근 실험 3개의 진행과 판정이 남고, 상단 막대에 "A/B 실험"
  진행이 보입니다.
- **한 번 시행 (결과표)** — 지금 파리로 정해진 시간만 돌려 결과표에 모읍니다(`out/sandbox/trials.csv`,
  열이 바뀌면 이전 파일은 `trials_until_<시각>.csv`로 남음).
- **설명**

```bash
.venv\Scripts\python.exe scripts\09_web_viewer.py --sandbox --port 8010
```

```bash
.venv\Scripts\python.exe scripts\17_run_experiment.py --set shock_odour --flies 8
```

실험 기록은 `out/experiments/<시각>-<세트>/`(설계·시행 기록·다시 보기·CSV·보고서)에 남고 뷰어가 목록으로
읽습니다. **실험은 사용자가 필요할 때 시작합니다** — 검증용 시범 실행도 묻지 않고 돌리지 않습니다. 세트별 시범
결과와 아직 돌려 보지 않은 세트는 PLAN.md E절. GPU는 재 보니 이 규모에서 CPU 병렬보다 이득이 작아 쓰지
않습니다(RESEARCH.md 9.1).

색 조건화 실험(A)은 사용자 요청으로 적당히 마무리 — 망각 시정수 문제를 찾아 고쳤고(1시간), 큰 스윕은
돌리지 않았습니다.

---

# A. 버섯체 연합학습

계획서: `~/.claude/plans/wondrous-sparking-engelbart.md` (승인됨).
**6단계 전부 완료했고 모든 스크립트를 실제로 실행해 검증했습니다.** 이어서 능동적
소거(계획서 밖, 사용자 승인)와 기억 라이브러리(사용자 요청)를 추가했습니다.

## 소거 실험 — 한눈에

3조건 × 5마리, 차등 조건화, 7.5분 (`out/mb_extinction/`,
그림 `out/analysis/mushroom_body_mb_extinction.png`):

| 조건 | 획득 후 | 역전 후 | 대칭성 | 역전 시험 직전 A: 가치 / approach / avoid |
|---|---:|---:|---:|---|
| 소거 없음 | +0.49 ± 0.01 | −0.30 ± 0.00 | 0.61 | −0.128 / 0.872 / 1.000 |
| **소거 → avoid** | **+0.48 ± 0.01** | **−0.50 ± 0.02** | **1.04** | −0.001 / **0.872** / 0.873 |
| 소거 → approach | −0.01 ± 0.04 | +0.06 ± 0.01 | — | −0.980 / 0.020 / 1.000 |

- 역전 기준(< −0.4) **통과**. 원래 기억(approach 0.872)은 그대로 두고 맞서는 기억이 상쇄.
- 잘못된 배선은 **양성 되먹임**으로 B의 −0.004 혐오를 노출 10회 만에 −0.65로 키워,
  벌받지 않은 B가 A보다 더 무서워짐. 자세한 추적은 RESEARCH.md 10번.
- 회로 검사 `13_mb_check.py` **16/16** (소거 항목 4개 추가).

## 기억 라이브러리

```bash
.venv\Scripts\python.exe scripts\16_memory.py list
.venv\Scripts\python.exe scripts\09_web_viewer.py --memory fear-generalized --port 8011
```

`memories/`에 5개 보관: `naive`, `a-punished`, `reversed-passive`,
`reversed-extinction`, `fear-generalized`. 파일마다 가중치·배선 시드·초파리 설정·저장
당시 가치가 들어 있고, 불러오면 기록과 소수점 6자리까지 일치합니다(확인함).
다시 만들려면 `16_memory.py curate --force`(소거 실험 결과가 `out/mb_extinction/`에
있어야 함).

## 첫 실험(구획) — 한눈에

3조건 × 5마리 × 58시행, 16코어 병렬 8.6분 (`out/mb/`, 그림 `out/analysis/mushroom_body.png`):

| 조건 | 순진 | A에 벌 준 뒤 | B로 역전한 뒤 | 가치 분리 | 훈련 시행당 벌 |
|---|---:|---:|---:|---:|---:|
| 구획 2개 | −0.05 ± 0.07 | **+0.49 ± 0.02** | **−0.30 ± 0.01** | +0.392 | 0.33초 |
| 전역 도파민 1개 | −0.05 ± 0.07 | +0.01 ± 0.13 | −0.04 ± 0.05 | **0.000** | 2.30초 |
| 가소성 없음 | −0.05 ± 0.07 | +0.01 ± 0.13 | −0.04 ± 0.05 | **0.000** | 2.30초 |

| 계획서 기준 | 결과 |
|---|---|
| 순진 \|PI\| < 0.1 | ✅ −0.05 |
| 획득 후 PI > +0.4 | ✅ +0.49 (5마리 모두 +0.44~+0.52) |
| 전역 도파민은 두 계약을 동시에 못 배움 | ✅ 가치 분리 정확히 0 |
| 역전 후 20시행 안에 PI < −0.4 | ❌ **−0.30** (5마리 모두 부호는 뒤집힘) |

**역전이 약한 원인** — 이 회로가 기억을 지우는 방법은 100초 시정수의 수동적
복원뿐입니다. 역전 시험이 시작될 때 A에 대한 접근 반응이 0.85까지만 돌아와 있어
초파리가 두 냄새를 모두 조금씩 싫어합니다. 구동력 차이 비율(0.67)과 선호도
비율(0.61)이 맞아떨어집니다. **기준을 맞추려고 파라미터를 조정하지 않았습니다** —
그러면 이 메커니즘이 가려집니다. 자세한 분석은 [RESEARCH.md](RESEARCH.md) 10번.

→ 이 원인은 위의 **소거 실험**으로 해결했습니다.

## 만든 것

| 파일 | 내용 | 검증 |
|---|---|---|
| [flyplay/olfactory.py](flyplay/olfactory.py) | ORN → 촉각엽 → KC 희소 부호화 | ✅ |
| [flyplay/mushroom_body.py](flyplay/mushroom_body.py) | 구획, KC→MBON 가소성, DAN | ✅ |
| [flyplay/conditioning.py](flyplay/conditioning.py) | 시행/세션 러너, 조향, 차등 조건화, 소거, 기억 저장·불러오기 | ✅ 전체 실험 2회 |
| [scripts/13_mb_check.py](scripts/13_mb_check.py) | 회로 pass/fail 16항목 | ✅ **16/16** |
| [scripts/14_run_conditioning.py](scripts/14_run_conditioning.py) | 실험 실행 (병렬), `--experiment compartments/extinction` | ✅ 스모크 + 전체 |
| [scripts/15_analyze_mb.py](scripts/15_analyze_mb.py) | 표 + 4분할 그림, 대칭성·회로 상태 표 | ✅ 두 실험 모두 |
| [scripts/16_memory.py](scripts/16_memory.py) | 기억 보관·목록·상세 | ✅ |
| [scripts/09_web_viewer.py](scripts/09_web_viewer.py) `--conditioning`, `--memory` | 실험 설명 · KC 래스터 · MBON · 가치, 기억 불러오기 | ✅ 브라우저에서 확인 |

14·15·뷰어는 위임한 에이전트가 썼고, 재개 후 전부 읽고 실행해 검증했습니다.
그 과정에서 고친 것:

- **시행 루프가 두 벌이었습니다.** 뷰어가 `run_trial`을 통째로 복제해 두어서
  조향 방식을 바꾸자 뷰어만 깨졌습니다. 이제 `ConditioningExperiment.trial_steps()`
  제너레이터 하나를 `run_trial`과 뷰어가 함께 씁니다. 세션 구성도 `session_plan()`
  하나로 합쳤습니다. 리팩터 전후로 시드 0의 첫 시험 선호도가 −0.516으로 같습니다.
- **분석 4번 패널의 설명이 틀렸습니다.** "전역 도파민은 두 냄새를 함께 누른다"가
  아니라 **짝지어진 냄새의 두 구획을 똑같이** 누릅니다. 패널을 접근/회피 구획
  비교로 바꿨습니다.
- **뷰어** — 영상 드래그가 이미지 끌기로 먹히던 것, 서버 재시작 직후 페이지가
  잘못된 모드로 고정되던 것, 캔버스 크기가 레이아웃에 되먹여 좁은 화면에서
  패널이 겹치던 것, 캡션 말줄임, 4:3 렌더의 큰 레터박스를 고쳤고, 영상 범례와
  실험 설명(블록 타임라인 + 지금 볼 것)을 넣었습니다.

## 측정한 것

**후각 전단** (`13_mb_check.py`, 배선 시드 8개, 농도 0.0016→0.11):

| 항목 | 값 | 기준 |
|---|---:|---|
| 같은 냄새, 농도 69배 변화 | **0.989** | > 0.9 |
| 냄새 A 대 B | **0.051** (최악 0.180) | < 0.2 |
| A와 B가 공유하는 KC | 8.2% | — |
| 활성 KC | 100 / 2000 (5.0%) | = k |

**버섯체** (합성 시행, 8초 중 4초 도파민):

| 항목 | 값 |
|---|---|
| CS+ 20시행 후 MBON_approach | **−69.3%** |
| 같은 기간 CS− | −1.1% |
| 다른 구획 | 0.0% |
| 역전 교차 시점 | 2시행 |
| 도파민 없이 120초 방치 | 0.987로 복원 |
| **전역 DAN 하나일 때 가치 분리** | **정확히 0.000** |

마지막 줄이 이 주제의 핵심 결과입니다. 근사값이 아니라 **항등식**입니다 — 스칼라
신호 하나는 두 구획에 같은 입력을 주므로 가중치가 갈라질 수 없습니다.

**폐루프** (실제 몸, 전체 실험 — 맨 위 표 참고)

**자기제한적 회피.** 기준 조건의 훈련 시행당 도파민은 첫 5시행 0.76초에서
마지막 5시행 0.21초로 줄어듭니다. A를 피하는 법을 배우면 냄새 구역에 덜 들어가니
벌도 덜 받고, 그래서 기억이 더 깊어지지 않고 일정 수준에서 멈춥니다(접근 MBON
최저 0.45~0.52). 탐색 단계의 한 마리(시드 0)에서는 6시행째부터 0초까지 내려갔지만
5마리 평균으로는 0이 되지 않습니다. 가소성이 없거나 전역 도파민인 조건은
2.25초 → 2.45초로 줄지 않습니다.

**시행 하나의 선호도는 거의 이진값입니다.** 초파리가 처음 잡은 냄새 기울기 쪽으로
곧장 가버리기 때문에 순진한 개체의 시행별 표준편차가 약 0.5입니다. 개체 간 평균의
표준오차는 학습 후 0.02로 작아집니다(학습 없는 조건은 0.13). 학습이 없으면 어느
쪽으로 갈지가 우연에 맡겨지고, 학습된 가치가 생기면 그것이 선택을 정해 주기
때문으로 보입니다 — 해석이고 따로 검증하지는 않았습니다.

## 설계 중 뒤집힌 판단 세 가지

계획서에 적었던 것과 실제가 달랐던 지점들입니다. 재개할 때 되돌리지 마세요.

**촉각엽 분할 정규화는 KC 부호를 바꾸지 못합니다.** `r/(σ + Σr)`의 분모는
스칼라라 PN 벡터 전체를 같은 비율로 줄일 뿐이고, top-k는 크기에 불변입니다.
σ를 0에서 1.0까지 쓸어도 KC 부호는 **글자 그대로 하나도 안 바뀝니다**(측정).
농도 불변성은 ORN 힐 포화가 작동 범위에서 거의 선형이라 생기는 것입니다. 진짜
Olsen–Wilson 형태(`r^1.5/(r^1.5+σ^1.5+(m Σr)^1.5)`)는 구획별로 작동해서 부호를
실제로 바꾸는데, **써봤더니 더 나빴습니다** — 불변성 0.989 → 0.822.

**조향 이득은 12가 아니라 80입니다.** `env_multimodal.py`의 ±0.04는 측면
발생원 기준이고, 정면에서는 좌우차가 **0.0006**이라 어떤 이득을 줘도 조향이
안 됩니다. 그래서 훈련 시행도 발생원을 정면에 두지 않고 ±35° 안의 무작위
방위에 둡니다. 정면에 두면 회피를 학습해도 그것을 행동으로 보일 방법이 없습니다.

**가치는 냄새별로 곱해야 합니다.** 좌우차를 냄새 차원끼리 더한 뒤 혼합물의
가치 하나를 곱하는 방식은 시험 시행에서 반드시 실패합니다 — A가 왼쪽, B가
오른쪽이면 두 기울기가 상쇄돼 합이 0이 되고, 초파리는 A를 싫어해도 그 차이를
쓸 수 없습니다. 지금은 `turn ∝ Σ_d (innate + valence_d) · asym_d · share_d`로,
가치로 가중한 세기의 기울기를 타고 올라갑니다.

## 다시 돌리기

```bash
.venv\Scripts\python.exe scripts\13_mb_check.py
```

회로 12항목, 몇 초. 여기가 깨지면 아래는 볼 필요가 없습니다.

```bash
.venv\Scripts\python.exe scripts\14_run_conditioning.py
```

전체 실험. 16코어에서 8.6분이고 `out/mb/`의 이전 결과를 지우고 새로 씁니다.
빠른 확인은 `--seeds 1 --n-train 3 --n-test 2`(1.3분).

```bash
.venv\Scripts\python.exe scripts\15_analyze_mb.py
```

```bash
.venv\Scripts\python.exe scripts\09_web_viewer.py --conditioning --port 8010
```

한 마리가 배우는 과정을 브라우저에서 봅니다. 기본 프로토콜은 12시행 훈련 /
4시행 시험(36시행)이라 전체 실험(20/6)보다 짧습니다.

## 비용

시행 하나가 단독 실행 시 약 4.3초, 15개 병렬 시 7.6~8.8초입니다(병렬에서 왜
절반 속도가 되는지는 측정하지 않았습니다). 전체 실험 870시행이 8.6분으로,
직렬 추정 62분 대비 7배 빠릅니다.

## 다음 작업을 위한 사전 조사

### ① 시각 연결

**FlyGym 겹눈이 색을 읽는 방식** (`flygym/vision/retina.py` 223–235행, 소스로 확인).
낱눈마다 종류가 하나이고, 종류에 따라 RGB 중 한 채널만 읽습니다.

| 낱눈 종류 | 개수(한 눈) | 읽는 RGB 채널 | 출력 채널 |
|---|---:|---|---:|
| 노랑형 (yellow) | 505 | **초록** | 0 |
| 옅은형 (pale) | 216 | **파랑** | 1 |
| — | — | **빨강은 어느 낱눈도 읽지 않음** | — |

그래서 **파랑과 초록은 구별할 수 있고, 빨간 물체는 검은색과 똑같이 보입니다.**
흰 하늘을 배경으로 파란 기둥은 노랑형 낱눈에만, 초록 기둥은 옅은형 낱눈에만 어둡게
잡힐 것입니다(예상, 아직 측정 안 함). 첫 후보 과제는 "파란 기둥 대 초록 기둥"
색 연합학습입니다.

아직 정해지지 않은 것: 시각 부호를 기존 케니언 세포와 섞을지 따로 둘지, 시각
특징을 무엇으로 만들지(색 비율 / 거친 망막 지도), 조향에 시각 가치를 어떻게 넣을지.
겹눈 렌더가 25 ms라 실험 시간이 약 2배가 됩니다.

### ② 밀폐된 방 실험 — 비판과 수정안

사용자 제안: 밀폐된 방, 냄새 나는 설탕물, 방해물, 길 찾기. 지적한 문제 (심각한 순):

1. **냄새가 벽을 통과함** — `OdorField.read`가 직선거리로 계산. 기울기가 벽 너머를
   가리키고 오목한 장애물 안에서 갇힘 → "길 찾기"가 아니라 "갇히는 빈도"를 재게 됨.
2. 비교 대상이 없으면 시연일 뿐 "무엇을 배웠나"가 아님.
3. 벽이 시각 특징의 수평선 보정을 망가뜨릴 가능성 (예상, 측정 필요).
4. 학습된 먹이 탐색 정책은 열린 공간·기둥 2개에서만 배워 옮겨지지 않음.
5. 우회율 분모는 직선거리가 아니라 장애물을 피한 최단 경로여야 함.
6. 배치 고정 시 경로 암기. 7. 부착력 켜진 발로 장애물을 기어오를 가능성(확인 필요).

수정안: 80×80 mm 방, 무작위 장애물(연결성 보장, 오목한 함정 포함), **장애물을 돌아가는
최단거리로 계산한 냄새장**, 조건 = 냄새만/시각만/둘 다 × 직선거리 냄새/우회거리 냄새,
지표 = 도달률·경로÷최단 우회경로·충돌·갇힌 시간. 학습 버전은 설탕물(A)과 맹물(B)을
두고 설탕 = 보상 도파민으로 A에 양의 가치를 붙인 뒤, 장애물을 돌아 A를 찾는지와
위치를 바꿔도 냄새를 따라가는지를 봄.

---

# B. 다중감각 먹이 탐색 커리큘럼

## 한눈에

**1·2단계 학습 완료, 3단계 미시작.** 2026-09-16 23:12에 시작한 3단계 커리큘럼이
2단계까지 끝내고(00:14:06에 `model.zip` 저장) 3단계 환경을 만들던 중 수동
중지되었습니다. 3단계는 체크포인트가 하나도 없는 상태였으므로 **손실된 학습은
없습니다.**

| 단계 | 이름 | 지형 / 기둥 | 스텝 | 상태 | 소요 |
|---|---|---|---:|---|---:|
| 1 | `forage_odor` | flat / 0개 | 150,528 / 150,000 | **완료** | 19.4분 |
| 2 | `forage_avoid` | flat / 2개 | 251,904 / 250,000 | **완료** | 41.5분 |
| 3 | `forage_rough` | blocks / 2개 | 0 / 250,000 | 미시작 | — |

체크포인트 49개(10.8 + 18.1 MB), `out/` 전체 49 MB. 목표 스텝을 살짝 넘는 것은
PPO가 롤아웃 단위(512 × 6환경 = 3,072 스텝)로만 끊기 때문입니다.

**뷰어:** 2단계 정책을 보려면 아래 명령으로 띄웁니다(지금은 꺼져 있음).

```bash
.venv\Scripts\python.exe scripts\09_web_viewer.py --follow forage_avoid --port 8000
```

---

## 재개하기

```bash
.venv\Scripts\python.exe scripts\11_train_forage.py --resume --steps 150000 250000 250000 --n-envs 6 --checkpoint-every 8000
```

처음 실행과 **같은 `--steps`**를 줘야 목표치가 맞습니다. 위 명령은 1·2단계를
건너뛰고 3단계(blocks 지형)부터 시작합니다. 예상 소요 40~50분.

`--resume`이 하는 일:

- 각 단계를 **자기 체크포인트와 `model.zip` 중 더 앞선 쪽**에서 재개합니다.
  완주한 단계는 `model.zip`이 마지막 체크포인트보다 앞서 있고, 중단된 단계는
  그 반대라서 둘 다 확인해야 합니다.
- 목표에 도달한 단계는 **환경을 만들지도 않고 건너뜁니다.** 판정은 zip 내부
  json의 `num_timesteps`만 읽어서 하므로 모델 로드 비용이 없습니다.
- **체크포인트를 지우지 않습니다.** 뷰어의 감각 기여도 학습 경과 그래프가
  이 파일들을 읽으므로 지우면 이력이 사라집니다.
- `reset_num_timesteps=False`로 스텝 카운터·텐서보드 곡선·체크포인트 번호가
  끊기지 않고 이어집니다.
- 체크포인트에는 가중치뿐 아니라 **옵티마이저 상태**도 들어 있어, 가중치만
  물려받는 단계 간 전이와 달리 같은 최적화를 그대로 잇습니다.

`--resume` 없이 돌리면 예전처럼 새로 시작하되, 이제는 체크포인트를 몇 개
지우는지 먼저 출력합니다.

### 중간에 멈추기

체크포인트가 8,000 스텝(약 80초)마다 떨어지므로 아무 때나 멈춰도 그 이하만
잃습니다. 프로세스 트리를 자식부터 역순으로 종료하면 됩니다 — 아래
[운영 메모](#운영-메모) 참고.

### 학습 후 할 일

```bash
.venv\Scripts\python.exe scripts\12_analyze_forage.py --run forage_rough
```

절제(ablation) 표와 조향 법칙 곡선을 뽑습니다. **아직 한 번도 실행하지
않았습니다** — 3단계가 끝나야 의미 있는 결과가 나옵니다.

---

## 학습 결과

### 보상 곡선 (`rollout/ep_rew_mean`)

| 스텝 | 1단계 `forage_odor` | 2단계 `forage_avoid` |
|---:|---:|---:|
| 3k | −190 | 114 |
| 77k | −143 | 132 |
| 114k | **+99** | 156 |
| 150k | **+205** | 165 |
| 224k | — | 185 |
| 252k | — | **+204** |

읽을 점 두 가지입니다.

**1단계는 약 100k 근처에서 주화성이 "켜졌습니다."** 그전까지 −190 ~ −140에
머물다 한 번에 양수로 넘어갔고, 에피소드 길이도 402 → 144 스텝으로 줄었습니다.
헤매다 시간 초과하던 것이 곧장 찾아가는 것으로 바뀐 지점입니다.

**2단계는 기둥이 추가되며 잃은 것을 전부 회복했습니다.** 1단계 최종 정책을
기둥 2개짜리 세계에 떨어뜨리자 205 → 114로 떨어졌고, 252k에서 203.9까지
돌아왔습니다. 에피소드 길이도 142 스텝으로 1단계(144)와 같습니다. 즉 **장애물을
피하면서도 이전만큼 빨리 먹이에 도달합니다.**

텐서보드로 직접 보려면:

```bash
.venv\Scripts\tensorboard.exe --logdir out/rl/tb
```

`forage_odor_4`와 `forage_avoid_1`이 실제 사용된 실행입니다
(`forage_odor_1~3`은 중단된 시도).

### 감각 기여도

뷰어가 체크포인트를 전부 읽어 **같은 상태 표본 64개**에 각 정책을 물리고,
관측 블록마다 ±δ를 넣어 선회 명령 변화 |Δturn|을 측정합니다.

2단계 체크포인트 7k → 191k 구간에서 측정한 변화:

| 채널 | 7k | 191k |
|---|---:|---:|
| 냄새 좌우차 | 0.704 | 0.660 |
| **냄새 변화(시간 미분)** | **0.058** | **0.124** |
| 냄새 세기 | 0.009 | 0.007 |
| 시각 | 0.155 | 0.121 |

**냄새 변화 채널이 두 배 넘게 자랐습니다.** 정책이 순간적인 좌우 비대칭에만
의존하던 것에서 "움직이면서 냄새가 짙어지는지"를 함께 쓰는 쪽으로 이동하는
중입니다. 실제 초파리가 casting(좌우로 훑기)으로 시간 정보를 얻는 전략과
같은 방향입니다.

시각은 0.06~0.19 사이를 오르내리며 아직 뚜렷한 추세가 없습니다. 3단계에서
지켜볼 지점입니다.

뷰어를 띄우면 전체 구간(현재 31개 체크포인트)을 다시 계산해 막대그래프로
보여줍니다.

### 손수 만든 기준선 (2단계, 학습 전)

| 방식 | 도달 | 충돌 스텝 |
|---|---|---:|
| 냄새만 | 3 / 8 | 134 |
| 냄새 + 시각 | 8 / 8 | 3.1 |

학습된 정책은 두 번째를 이겨야 합니다. 이 대비가 **두 감각이 서로를 대체할 수
없다**는 과제 설계의 근거입니다.

---

## 파일 위치

```
out/rl/
  forage_odor/    model.zip  meta.json  checkpoints/ (18개)
  forage_avoid/   model.zip  meta.json  checkpoints/ (31개)
  forage_rough/   checkpoints/ (비어 있음)
  tb/             텐서보드 로그
```

`meta.json`에는 지형·기둥 수·목표 스텝이 들어 있습니다. 뷰어의 `--follow`가
이 파일을 읽어 어떤 환경을 만들지 정하므로, 손으로 고치면 뷰어가 잘못된
환경을 띄웁니다.

---

## 운영 메모

이 PC에서 겪은 것들입니다. 재현되는 문제이므로 기록해 둡니다.

**프로세스 조회는 `Win32_Process`를 쓸 것.** `Get-Process python`이 학습
프로세스가 살아 있는데도 빈 결과를 돌려준 적이 있습니다. 실제 상태를 보려면:

```bash
powershell -c "Get-CimInstance Win32_Process -Filter \"Name LIKE '%python%'\" | Select-Object ProcessId, ParentProcessId, CommandLine | Format-List"
```

**`taskkill /T`는 권한 거부됩니다.** 프로세스 트리를 직접 훑어 자식부터
역순으로 `Stop-Process -Force` 해야 합니다. 그냥 부모만 죽이면 `SubprocVecEnv`
워커 6개가 고아로 남아 CPU를 계속 먹습니다.

**학습 프로세스는 2층입니다.** `.venv\Scripts\python.exe`(런처) →
`Python312\python.exe`(실제 학습) → 워커 6개. 런처 PID만 죽이면 아무것도
멈추지 않습니다.

**실행 중인 스크립트를 편집해도 반영되지 않습니다.** 파이썬은 시작 시점의
코드를 메모리에 들고 있습니다. 이번에도 `write_meta`를 고쳤지만 이미 돌던
프로세스는 옛 코드로 `meta.json`을 써서 `status` 키가 빠졌습니다.

---

## 다음 할 일

1. **3단계 학습** — 위 `--resume` 명령. 40~50분.
2. **`scripts/12_analyze_forage.py --run forage_rough`** — 절제 표(둘 다 /
   냄새만 / 시각만)와 조향 법칙 곡선. 승인된 계획의 최종 산출물입니다.
3. **`scripts/08_gpu_benchmark.py`** — CPU가 한가할 때 한 번도 제대로 돌린 적이
   없습니다.

---

# 두 갈래의 우선순위

A(버섯체)와 B(먹이 탐색)는 **서로 의존하지 않습니다.** 같은 `flyplay.odor`와
`flyplay.build`를 쓰지만 환경도 학습 방식도 별개라, 어느 쪽을 먼저 재개해도
다른 쪽이 깨지지 않습니다. 다만 둘 다 CPU를 다 쓰므로 **동시에 돌리지는
마세요** — 물리 시뮬레이션이 서로 느려집니다.

A가 더 앞서 있고(회로 검증 완료, 남은 미지수 하나), B는 40분짜리 학습을 한 번
더 돌리면 되는 상태입니다.
