# Everything the scripts import, at the versions they were tested with:
# Windows 11, Python 3.12.10. FlyGym 2.1 needs Python 3.12 to 3.14.
#
# On Linux, pip's default torch is the CUDA build (several GB). Install the CPU
# build first, then this file:
#   python -m pip install torch==2.14.0 --index-url https://download.pytorch.org/whl/cpu
#
# The GPU benchmark (scripts/08_gpu_benchmark.py) needs requirements-gpu.txt too.

flygym[rl,examples]==2.1.0
mujoco==3.9.0
numpy==2.5.3
numba==0.67.0
scipy==1.18.1
matplotlib==3.11.2
pillow==12.3.0
imageio==2.37.4
PyYAML==6.0.3
tqdm==4.70.1
psutil==7.2.2

# Reinforcement learning (scripts 05-07 and 11-12, the viewer's --policy and --follow)
gymnasium==1.3.0
stable-baselines3==2.9.0
tensorboard==2.21.0
torch==2.14.0
